// Phase 0 · Lesson 01 — TypeScript entrypoint for the dev environment verifier.
// Keeps verify.ts as the compatibility script while exposing a main.* entry.
// Default mode reports diagnostics and exits 0; pass --strict to fail required gaps.
// Refs: https://nodejs.org/api/process.html

import "./verify.ts";
