# 第 17 轮 Git Collaboration 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Git 与协作课程的旧 quiz 分布，并润色中文图示与术语表。

**Architecture:** 本轮只改 `phases/00-setup-and-tooling/02-git-and-collaboration` 下的中文文档和中英 quiz。课程没有 `code/`，因此不新增 tests。保留 git 命令、分支名、文件扩展名等代码/命令文本；普通概念标签中文优先。

**Tech Stack:** Markdown、JSON、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Git Collaboration 课程

**Files:**
- Modify: `phases/00-setup-and-tooling/02-git-and-collaboration/docs/zh-CN.md`
- Modify: `phases/00-setup-and-tooling/02-git-and-collaboration/quiz.json`
- Modify: `phases/00-setup-and-tooling/02-git-and-collaboration/quiz.zh-CN.json`

- [x] **Step 1: 润色中文图示和术语表**

中文化 Mermaid 参与者和术语表第一列；保留 git 命令和 `.gitignore` 等文件名。

- [x] **Step 2: 收敛并润色 quiz**

将中英 quiz 从 5 题调整为 6 题，并调整为：
- 1 pre：version control 的核心作用。
- 3 check：repository、add/commit/push、branch creation。
- 2 post：model checkpoint ignore、`git log --oneline` 的用途。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 3: 单课校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/02-git-and-collaboration
git add phases/00-setup-and-tooling/02-git-and-collaboration/docs/zh-CN.md phases/00-setup-and-tooling/02-git-and-collaboration/quiz.json phases/00-setup-and-tooling/02-git-and-collaboration/quiz.zh-CN.json
git commit -m "fix(phase-00/02): polish git collaboration lesson"
```

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 17 轮审查.md`
- Add: `cowork/plan/第 17 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/00-setup-and-tooling/02-git-and-collaboration&lang=zh-CN`。确认 H1、中文 Mermaid、quiz 分布和交互、控制台日志正常；复验后还原不应提交的生成文件。

- [ ] **Step 3: 提交并推送第 17 轮记录**

```bash
git add -f cowork/review/第\ 17\ 轮审查.md cowork/plan/第\ 17\ 轮修改计划.md
git commit -m "docs(review): add round 17 review and plan"
git push
```

## Execution Record

- Lesson commit: `56f791b7 fix(phase-00/02): polish git collaboration lesson`
- Single-lesson validation: `python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/02-git-and-collaboration` passed with 0 issues.
- Quiz contract: `quiz.json` and `quiz.zh-CN.json` both have 6 questions with `pre=1/check=3/post=2`, top-level `lesson`, and top-level `title`.
- Full validation passed:
  - `python3 scripts/audit_lessons.py`
  - `python3 scripts/i18n_validate.py`
  - `node --test site/tests/*.test.mjs`
- Browser QA URL: `http://127.0.0.1:4187/lesson.html?path=phases/00-setup-and-tooling/02-git-and-collaboration&lang=zh-CN&qa=20260609r17b`
- Browser QA evidence:
  - Page identity: title `Git 与协作 - AI Engineering from Scratch`, H1 `Git 与协作`.
  - Console health: no warn/error logs.
  - Rendered content: 1 Mermaid source rendered to 1 SVG; visible diagram labels include `工作目录`、`暂存区`、`本地仓库`、`远端（GitHub）`.
  - Quiz sections: pre has 1 question and 4 options; check has 3 questions and 12 options; post has 2 questions and 8 options.
  - Quiz interaction: clicking `随时间追踪文件变更，并与他人协作` marks one correct option, disables 4 options, displays the explanation, and updates score to `1/1 题正确`.
  - Runtime panel: no runtime entry is expected because this lesson has no code directory.
  - Screenshot note: full-page screenshot timed out once in Browser; a clipped screenshot succeeded but its clip origin was page-relative, so DOM and interaction state are the primary evidence.
- Generated-file hygiene: ran `node site/build.js` for local QA and restored generated `site/data.js` before committing records.
