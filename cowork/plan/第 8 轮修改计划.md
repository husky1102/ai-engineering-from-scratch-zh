# 第 8 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 8 轮审查发现的 Inference Optimization 课程半翻译问题，让中文正文、图示、表格、quiz 和页面元数据更自然、准确、完整。

**Architecture:** 本轮只改 `phases/10-llms-from-scratch/12-inference-optimization` 下的中文文档和中文 quiz。保留 Prefill、Decode、KV cache、PagedAttention、EAGLE、TensorRT-LLM、ops:byte、TPS、模型名、引擎名、公式和代码标识；把普通解释性英文改成中文。单课校验和 Python/Rust 代码运行后按课程单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python/Rust 课程代码、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Inference Optimization 课程

**Files:**
- Modify: `phases/10-llms-from-scratch/12-inference-optimization/docs/zh-CN.md`
- Modify: `phases/10-llms-from-scratch/12-inference-optimization/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、表格和 Mermaid**

中文化标题、导语、学习目标、问题段、核心概念、Mermaid 可见标签、speculative decoding 表、engine 表、ops:byte 表、动手步骤、交付成果、练习、术语表和延伸阅读说明。中文页面语言字段补充 Rust。保留 Prefill、Decode、KV cache、continuous batching、PagedAttention、speculative decoding、prefix caching、EAGLE、TensorRT-LLM、RadixAttention、ops:byte、TPS、A100、H100、BF16、FP8 等术语。

执行记录：已中文化标题、导语、目标、普通叙述、Mermaid 可见标签、表格列名、步骤标题、练习和术语表；课程语言字段补充 `Rust`，与本课 `code/main.rs` 入口一致。

- [x] **Step 2: 润色 quiz**

把 quiz 中的解释性英文改为中文，例如 `Training and evaluation`、`prompt tokens`、`autoregressive`、`model weights`、`memory bandwidth`、`generation step`、`previous tokens`、`virtual memory`、`fixed-size blocks`、`candidate tokens`。保持原有数组结构、题目数量、`stage` 和 `correct` 索引不变。

执行记录：已润色 5 道 quiz 的问题、选项和解释，保留原有数组结构、`stage` 和 `correct` 索引。

- [x] **Step 3: 单课校验、运行代码并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/10-llms-from-scratch/12-inference-optimization
python3 phases/10-llms-from-scratch/12-inference-optimization/code/main.py
rustc --edition 2021 phases/10-llms-from-scratch/12-inference-optimization/code/main.rs -o /tmp/inference-optimization && /tmp/inference-optimization
git add phases/10-llms-from-scratch/12-inference-optimization/docs/zh-CN.md phases/10-llms-from-scratch/12-inference-optimization/quiz.zh-CN.json
git commit -m "fix(phase-10/12): polish zh-CN inference optimization"
```

执行记录：`python3 scripts/i18n_validate.py --lesson phases/10-llms-from-scratch/12-inference-optimization` 返回 `docs 1/1, quiz 1/1, 0 issue(s)`；残留扫描未再命中第 8 轮列出的英文标题、表头、Mermaid 标签和 quiz 短语，剩余 `Static batching` 为保留术语。系统 `python3` 缺少 numpy，因此用项目 `.venv/bin/python` 运行 Python demo，退出 0；`rustc --edition 2021 phases/10-llms-from-scratch/12-inference-optimization/code/main.rs -o /tmp/inference-optimization && /tmp/inference-optimization` 退出 0。课程翻译提交为 `fbf5a6fb fix(phase-10/12): polish zh-CN inference optimization`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 8 轮审查.md`
- Add: `cowork/plan/第 8 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：`audit_lessons.py` 返回 `503 lesson(s) checked, 0 issue(s)`；`i18n_validate.py` 返回 `docs 503/503, quiz 338/338, 0 issue(s)`；`node --test site/tests/*.test.mjs` 返回 4 个测试全部通过。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/10-llms-from-scratch/12-inference-optimization&lang=zh-CN`。确认 H1、关键中文短语、Mermaid、表格、代码高亮、运行按钮、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：`node site/build.js` 后用临时服务打开第 8 轮课程页，控制台 `warn/error` 为 0；正文 H1 为 `推理优化`，正文范围包含 `Prefill 与 Decode`、`没有 KV Cache`、`使用 KV Cache`、`连续分配`、`候选 token`、`推理引擎`、`关键术语`，且排除导航/AI 面板后的正文未命中第 8 轮列出的英文残留。页面渲染 5 个 Mermaid SVG、5 个表格、13 个代码块，其中 9 个 Python 块有站点自带 `.syn-*` 高亮 token；本课 runtime panel 提供 Python/Rust 本地运行命令，但未启用内联 Pyodide 运行按钮，终端中 Python/Rust demo 已验证通过。本课无公式，KaTeX 节点为 0 且无未渲染公式痕迹。复验后已停止临时服务并还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 8 轮记录**

```bash
git add -f cowork/review/第\ 8\ 轮审查.md cowork/plan/第\ 8\ 轮修改计划.md
git commit -m "docs(review): add round 8 review and plan"
git push
```

执行记录：计划文件更新后提交并推送本轮记录。
