# 第 7 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 7 轮审查发现的 Embeddings 课程半翻译问题，让中文正文、图示、表格和 quiz 更自然、准确、完整。

**Architecture:** 本轮只改 `phases/11-llm-engineering/04-embeddings` 下的中文文档和中文 quiz。保留嵌入模型、论文、API 参数、产品名和代码标识；把普通解释性英文改成中文。单课校验和代码运行后按课程单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python/TypeScript 课程代码、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Embeddings 课程

**Files:**
- Modify: `phases/11-llm-engineering/04-embeddings/docs/zh-CN.md`
- Modify: `phases/11-llm-engineering/04-embeddings/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、表格和 Mermaid**

中文化标题、导语、学习目标、问题定义、核心概念标题、模型对比表、相似度指标表、HNSW 图、分块策略表、bi-encoder/cross-encoder 图、练习、术语表和延伸阅读说明。保留 Word2Vec、Sentence-BERT、MTEB、HNSW、Matryoshka、BGE-M3、Qwen3-Embedding、OpenAI、Cohere、Voyage AI、FAISS、pgvector、`search_query:`、`text-embedding-3-small` 等专有名词和代码标识。

执行记录：已中文化标题、导语、目标、普通叙述、表格列名、Mermaid 可见标签、练习和术语表；课程语言字段补充 `TypeScript`，与本课 `code/main.ts` 的可运行入口一致。

- [x] **Step 2: 润色 quiz**

把 quiz 中的解释性英文改为中文，例如 `keyword search`、`semantic meaning`、`high-dimensional vectors`、`word choice`、`dimensionality`、`retrieval benchmarks`、`data privacy`、`offline operation`。保持原有数组结构、题目数量、`stage` 和 `correct` 索引不变。

执行记录：已润色 5 道 quiz 的问题、选项和解释，保留原有数组结构、`stage` 和 `correct` 索引。

- [x] **Step 3: 单课校验、运行代码并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/11-llm-engineering/04-embeddings
npx tsx phases/11-llm-engineering/04-embeddings/code/main.ts
git add phases/11-llm-engineering/04-embeddings/docs/zh-CN.md phases/11-llm-engineering/04-embeddings/quiz.zh-CN.json
git commit -m "fix(phase-11/04): polish zh-CN embeddings lesson"
```

执行记录：`python3 scripts/i18n_validate.py --lesson phases/11-llm-engineering/04-embeddings` 返回 `docs 1/1, quiz 1/1, 0 issue(s)`；残留扫描未再命中第 7 轮列出的英文标题/表头/quiz 短语；`npx tsx phases/11-llm-engineering/04-embeddings/code/main.ts` 退出 0，并跑完 chunking、embedding、similarity metrics、semantic search、Matryoshka truncation 和 binary quantization demo。课程翻译提交为 `c8150d0d fix(phase-11/04): polish zh-CN embeddings lesson`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 7 轮审查.md`
- Add: `cowork/plan/第 7 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：`audit_lessons.py` 返回 `503 lesson(s) checked, 0 issue(s)`；`i18n_validate.py` 返回 `docs 503/503, quiz 338/338, 0 issue(s)`；`node --test site/tests/*.test.mjs` 返回 4 个测试全部通过。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/11-llm-engineering/04-embeddings&lang=zh-CN`。确认 H1、关键中文短语、Mermaid、表格、代码高亮、运行按钮、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：`node site/build.js` 后用临时服务打开第 7 轮课程页，控制台 `warn/error` 为 0；正文 H1 为 `嵌入与向量表示`，包含 `词汇不匹配问题`、`现代 Embedding 模型`、`相似度指标`、`向量数据库与 HNSW`、`分块策略`、`关键术语`。正文区域不再包含第 7 轮审查列出的英文标题/表头/quiz 普通短语；3 个 Mermaid SVG、5 个表格和 quiz 均正常。页面有 14 个代码块，其中 10 个 Python 块有站点自带 `.syn-*` 高亮 token，并挂载 10 个 `运行` 按钮；点击第一个 Python 代码块运行按钮后状态为 `done`、按钮恢复可用、状态显示 `准备就绪。`。本课无公式，KaTeX 节点为 0 且无未渲染公式痕迹。复验后已停止临时服务并还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 7 轮记录**

```bash
git add -f cowork/review/第\ 7\ 轮审查.md cowork/plan/第\ 7\ 轮修改计划.md
git commit -m "docs(review): add round 7 review and plan"
git push
```

执行记录：计划文件更新后提交并推送本轮记录。
