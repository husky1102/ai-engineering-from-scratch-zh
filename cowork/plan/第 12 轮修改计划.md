# 第 12 轮 Runtime Feedback Loops 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Runtime Feedback Loops 课程的中文半翻译、quiz 数量不合约和本地网页 runtime package 显示问题。

**Architecture:** 本轮改 `phases/14-agent-engineering/37-runtime-feedback-loops` 下的中英文文档与中英 quiz，并修复 `site/lesson.html` 的 runtime package 展示。feedback、telemetry、runner、record、JSONL、stdout/stderr、exit_code、duration、verification gate、redaction、rotation、retry chain、parent_command_id、Claude Code、LangGraph、CI 等必要术语保留；普通解释性英文、图示标签、表格和术语表中文化。将中英 quiz 收敛到 6 题，并调整为 1 pre、3 check、2 post；课程修复按本课单独提交，站点修复单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib demo、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Runtime Feedback Loops 课程

**Files:**
- Modify: `phases/14-agent-engineering/37-runtime-feedback-loops/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/37-runtime-feedback-loops/docs/en.md`
- Modify: `phases/14-agent-engineering/37-runtime-feedback-loops/quiz.json`
- Modify: `phases/14-agent-engineering/37-runtime-feedback-loops/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、Mermaid、表格和术语表**

中文化标题、导语、学习目标、Mermaid 可见标签、字段表解释、截断说明、feedback/telemetry 区分、生产模式、实际使用、交付成果、练习、术语表和延伸阅读说明。保留必要术语和字段名。

- [x] **Step 2: 收敛并润色 quiz**

将中英 quiz 从 7 题收敛到 6 题，并调整为：
- 1 pre：feedback runner 强制代理响应事实。
- 3 check：必备 `exit_code`、确定性截断、写入时 redaction。
- 2 post：`parent_command_id`、1 MB rotation。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 3: 单课校验、运行 Python demo 并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/37-runtime-feedback-loops
python3 phases/14-agent-engineering/37-runtime-feedback-loops/code/main.py
rm -f phases/14-agent-engineering/37-runtime-feedback-loops/code/feedback_record.jsonl*
git add phases/14-agent-engineering/37-runtime-feedback-loops/docs/zh-CN.md phases/14-agent-engineering/37-runtime-feedback-loops/quiz.json phases/14-agent-engineering/37-runtime-feedback-loops/quiz.zh-CN.json
git commit -m "fix(phase-14/37): polish runtime feedback loops lesson"
```

执行记录：
- `python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/37-runtime-feedback-loops`：docs 1/1、quiz 1/1、0 issues。
- `python3 phases/14-agent-engineering/37-runtime-feedback-loops/code/main.py`：退出 0，输出 5 条 demo record 摘要。
- 清理：`rm -f phases/14-agent-engineering/37-runtime-feedback-loops/code/feedback_record.jsonl*`。
- quiz 阶段分布：`pre=1`、`check=3`、`post=2`。
- stale 文本扫描：`Runtime Feedback Loops`、`Agent Loop`、`shell expansion surprise`、`redaction pass`、`foot-gun`、`runtime cost`、`loader call`、`Run log`、`Two log files` 等旧残留均未在课程正文或中文 quiz 中残留。
- 额外修复：中英文运行命令 code fence 同步为 `bash`，满足 fenced code block 语言标签规则。
- `git diff --check`：退出 0。
- 本课提交：`27e9cd8f fix(phase-14/37): polish runtime feedback loops lesson`。

### Task 2: 修复本地站点 runtime package 展示

**Files:**
- Modify: `site/lesson.html`

- [x] **Step 1: 过滤标准库 package 展示**

在运行面板展示层过滤 `__future__`、`os`、`shlex`、`subprocess`、`sys`、`uuid`，避免将 Python 标准库 import 显示成需要安装的包。

- [x] **Step 2: 验证并提交站点修复**

```bash
node --test site/tests/*.test.mjs
git diff --check -- site/lesson.html
git add site/lesson.html
git commit -m "fix(site): hide stdlib runtime packages"
```

执行记录：
- `node --test site/tests/*.test.mjs`：4 tests、4 pass、0 fail。
- Browser 复验：运行面板不再显示 `包: __FUTURE__, SHLEX, SUBPROCESS, UUID`；仍显示本地运行命令和 subprocess note。
- 站点提交：`1216eac9 fix(site): hide stdlib runtime packages`。

### Task 3: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 12 轮审查.md`
- Add: `cowork/plan/第 12 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- 课程修复后：`python3 scripts/audit_lessons.py` 503 lessons checked、0 issues；`python3 scripts/i18n_validate.py` docs 503/503、quiz 338/338、0 issues；`node --test site/tests/*.test.mjs` 4 pass。
- 站点修复后重新验证：`python3 scripts/audit_lessons.py` 503 lessons checked、0 issues；`python3 scripts/i18n_validate.py` docs 503/503、quiz 338/338、0 issues；`node --test site/tests/*.test.mjs` 4 pass。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/14-agent-engineering/37-runtime-feedback-loops&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、表格、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- `node site/build.js`：生成 `site/data.js`、`sitemap.xml`、`llms.txt`，并镜像 3646 个 lesson content 文件；复验后还原不应提交的生成文件。
- 临时静态服务：`python3 -m http.server 4186 --bind 127.0.0.1`。
- Browser URL：`http://127.0.0.1:4186/lesson.html?path=phases/14-agent-engineering/37-runtime-feedback-loops&lang=zh-CN&v=round12-packagefix`。
- 页面 H1：`运行时反馈循环`。
- stale 正文检查：`Runtime Feedback Loops` 仅来自左侧导航的英文课程名，文章正文 H1 和主体未残留；`Agent Loop`、`shell expansion surprise`、`redaction pass`、`foot-gun`、`runtime cost`、`loader call`、`Run log`、`Two log files` 等均未在文章正文中残留。
- Mermaid：1 个 source block，1 个 `.mermaid-render svg`。
- 代码：运行命令 code fence 为 `bash`，页面可复制；源码卡显示 `python main.py`。
- quiz：3 个 section，6 道题，`quiz-pre=1`、`quiz-check=3`、`quiz-post=2`，三段均可见。
- 运行代码面板：显示本地运行命令 `python phases/14-agent-engineering/37-runtime-feedback-loops/code/main.py`；不显示标准库 import 为 packages；保留 `USES SUBPROCESS` / `IMPORTS SUBPROCESS` note。
- 数学渲染：本课无公式，KaTeX 0，未发现 raw math hints。
- 控制台：warning/error 日志 0。

- [x] **Step 3: 提交并推送第 12 轮记录**

```bash
git add -f cowork/review/第\ 12\ 轮审查.md cowork/plan/第\ 12\ 轮修改计划.md
git commit -m "docs(review): add round 12 review and plan"
git push
```
