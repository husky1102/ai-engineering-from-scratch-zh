# 第 15 轮 Multi-Session Handoff 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Multi-Session Handoff 课程的中文标签半翻译、quiz 数量不合约和代码测试缺口，并复验本地网页。

**Architecture:** 本轮只改 `phases/14-agent-engineering/40-multi-session-handoff` 下的中文文档、中英 quiz、Python tests；如网页复验发现站点问题，再按实际问题最小范围修复站点。保留 schema 字段、文件名和状态枚举；普通解释性英文中文化。将中英 quiz 收敛到 6 题，并调整为 1 pre、3 check、2 post。补齐 stdlib unittest 后按本课单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib demo、Python unittest、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Multi-Session Handoff 课程

**Files:**
- Modify: `phases/14-agent-engineering/40-multi-session-handoff/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/40-multi-session-handoff/quiz.json`
- Modify: `phases/14-agent-engineering/40-multi-session-handoff/quiz.zh-CN.json`
- Add: `phases/14-agent-engineering/40-multi-session-handoff/code/tests/`

- [x] **Step 1: 润色正文、Mermaid、清理检查表和术语表**

中文化 Mermaid 可见标签、清理状态表、生产模式、实际使用、练习、术语表和延伸阅读说明。保留必要字段名、文件名和状态枚举。

- [x] **Step 2: 收敛并润色 quiz**

将中英 quiz 从 7 题收敛到 6 题，并调整为：
- 1 pre：`next_action` 为什么是交接包承重字段。
- 3 check：human/machine 双格式、handoff vs compaction、收尾时机。
- 2 post：feedback log 裁剪、跨 agent 协作 metadata。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 3: 补齐 unittest、单课校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/40-multi-session-handoff
python3 phases/14-agent-engineering/40-multi-session-handoff/code/main.py
cd phases/14-agent-engineering/40-multi-session-handoff/code && python3 -m unittest discover tests -v
rm -f phases/14-agent-engineering/40-multi-session-handoff/code/handoff.md phases/14-agent-engineering/40-multi-session-handoff/code/handoff.json
git add phases/14-agent-engineering/40-multi-session-handoff/code/tests phases/14-agent-engineering/40-multi-session-handoff/docs/zh-CN.md phases/14-agent-engineering/40-multi-session-handoff/quiz.json phases/14-agent-engineering/40-multi-session-handoff/quiz.zh-CN.json
git commit -m "fix(phase-14/40): polish handoff lesson"
```

执行记录：
- `docs/zh-CN.md` 已中文化 Mermaid 的 `Next Session`、清理状态表、生产模式小标题、练习、术语表和延伸阅读说明。
- `quiz.json` 与 `quiz.zh-CN.json` 均收敛为 6 题，阶段分布为 `pre=1/check=3/post=2`。
- 新增 6 个 unittest，覆盖 feedback tail + nonzero 裁剪、重复失败去重、risk derivation、handoff markdown/payload shape、fallback `next_action`、`main()` 写出的 JSON/Markdown。
- `python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/40-multi-session-handoff`：0 issues。
- `python3 phases/14-agent-engineering/40-multi-session-handoff/code/main.py`：通过，生成的 `handoff.md`、`handoff.json` 已清理。
- `cd phases/14-agent-engineering/40-multi-session-handoff/code && python3 -m unittest discover tests -v`：6 tests OK。
- 提交：`c2c3f12c fix(phase-14/40): polish handoff lesson`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 15 轮审查.md`
- Add: `cowork/plan/第 15 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- `python3 scripts/audit_lessons.py`：`503 lesson(s) checked, 0 issue(s)`。
- `python3 scripts/i18n_validate.py`：`docs 503/503, quiz 338/338, 0 issue(s)`。
- `node --test site/tests/*.test.mjs`：5 tests passed。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/14-agent-engineering/40-multi-session-handoff&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、表格、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- `node site/build.js`：完成，刷新 `site/content/` 镜像；复验后 `site/data.js` 已还原。
- 页面：`http://127.0.0.1:4187/lesson.html?path=phases/14-agent-engineering/40-multi-session-handoff&lang=zh-CN`。
- 浏览器 DOM：H1 为 `多会话交接`；`mermaidSvgs=1`；`codeBlocks=1`；正文未命中 `Next Session|Working tree|Temp artifacts|Session-end hook|PR template|Cross-agent handoff|lingua franca|Handoff packet|Feedback trim|Status report|distributed-systems framing`。
- 运行面板：包含 `Python 使用浏览器运行器尚未启用的包。`，不包含原始英文 allowlist note；`codeContentHasPanelLoading=false`，runtime note 的 `textTransform=none`。
- Quiz DOM：`quiz-pre` 有 1 题，`quiz-check` 有 3 题，`quiz-post` 有 2 题。
- Quiz 交互：点击 `#pre-q0 .quiz-option[data-index="1"]` 后，`answered=true`、`disabledCount=4`、解释 `display=block`、分数 `1/1 题正确` 显示。
- 控制台：`warn/error` 日志为空。

- [ ] **Step 3: 提交并推送第 15 轮记录**

```bash
git add -f cowork/review/第\ 15\ 轮审查.md cowork/plan/第\ 15\ 轮修改计划.md
git commit -m "docs(review): add round 15 review and plan"
git push
```
