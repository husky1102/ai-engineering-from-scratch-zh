# 第 3 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 3 轮审查发现的两门高残留课程，让正文、表格、图示和 quiz 的中文自然度显著提升，同时保持代码块与协议字段准确。

**Architecture:** 本轮只改两门课程自己的 `docs/zh-CN.md` 和 `quiz.zh-CN.json`。代码块内的 API、类型名、JSON 字段和示例 prompt 字符串保持原样，优先改用户直接阅读的正文、表格、Mermaid 标签、步骤标题和 quiz 文案。每门课独立校验、独立提交；最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色生产级 LLM 应用课

**Files:**
- Modify: `phases/11-llm-engineering/13-production-app/docs/zh-CN.md`
- Modify: `phases/11-llm-engineering/13-production-app/quiz.zh-CN.json`

- [x] **Step 1: 润色正文和表格**

中文化 frontmatter、开头、学习目标、架构描述、表格表头、部署清单和步骤标题。保留 LLM、RAG、SSE、API、FastAPI、OpenTelemetry、模型名和代码标识符。

- [x] **Step 2: 润色 Mermaid 可见标签**

把架构图、流式 sequence diagram 和 A/B testing 图中的可见英文标签改为中文，保持 Mermaid 语法和节点 ID 不变。

- [x] **Step 3: 润色 quiz**

把 quiz 中的解释性英文短语改为中文，例如 `Infrastructure`、`error handling`、`provider outages`、`graceful degradation`、`observability metrics`、`rate limiting`，保持 `correct` 索引不变。

- [x] **Step 4: 校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/11-llm-engineering/13-production-app
git add phases/11-llm-engineering/13-production-app/docs/zh-CN.md phases/11-llm-engineering/13-production-app/quiz.zh-CN.json
git commit -m "fix(phase-11/13): polish zh-CN production app lesson"
```

### Task 2: 润色通信协议课

**Files:**
- Modify: `phases/16-multi-agent-and-swarms/03-communication-protocols/docs/zh-CN.md`
- Modify: `phases/16-multi-agent-and-swarms/03-communication-protocols/quiz.zh-CN.json`

- [x] **Step 1: 润色正文和表格**

中文化标题、先修、学习目标、问题陈述、协议说明、表格列名和步骤标题。保留 MCP、A2A、ACP、ANP、JSON-RPC、REST、gRPC、DID、E2EE、HPKE、状态枚举和 JSON 字段。

- [x] **Step 2: 润色 Mermaid 可见标签**

把协议 landscape、discovery、ANP 分层、信任流程和组合架构图中的可见英文说明改为中文，保持节点 ID 不变。

- [x] **Step 3: 润色 quiz**

把 quiz 中的解释性英文短语改为中文，例如 `training data`、`external servers`、`hardcode`、`task delegation`、`visual dashboard`、`type safety`、`interoperability`、`enterprise auditability`。

- [x] **Step 4: 校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/16-multi-agent-and-swarms/03-communication-protocols
git add phases/16-multi-agent-and-swarms/03-communication-protocols/docs/zh-CN.md phases/16-multi-agent-and-swarms/03-communication-protocols/quiz.zh-CN.json
git commit -m "fix(phase-16/03): polish zh-CN communication protocols"
```

### Task 3: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 3 轮审查.md`
- Add: `cowork/plan/第 3 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用新端口或缓存绕开方式检查两门课的 H1、关键中文短语、quiz 文案和控制台日志。复验后还原不应提交的生成文件。

执行记录：使用临时 `4176` 静态服务复验两门课。`13-production-app` 页面 H1 为 `构建生产级 LLM 应用`，出现 `生产架构`、`错误处理：三层`、`部署检查清单` 和中文 quiz 文案，未命中本轮旧英文目标。`03-communication-protocols` 页面 H1 为 `通信协议`，出现 `协议地图`、`任务生命周期` 和中文 quiz 文案；唯一旧目标命中来自协议全名 `Agent Communication Protocol`，不是标题残留。两页浏览器 warn/error 日志为空。复验后已还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 3 轮记录**

```bash
git add -f cowork/review/第\ 3\ 轮审查.md cowork/plan/第\ 3\ 轮修改计划.md
git commit -m "docs(review): add round 3 review and plan"
git push
```
