# 第 4 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 4 轮审查发现的 Guardrails 课程中文残留，让正文、图示、表格和 quiz 更自然、准确、完整，并保持代码/API/安全术语准确。

**Architecture:** 本轮只改 `phases/11-llm-engineering/12-guardrails` 下的中文文档和中文 quiz。正文与 quiz 做中文润色；代码块、正则、模型/API 名、库名、RFC/论文标题和攻击示例字符串保持可识别。先单课校验并按课程单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Guardrails 课程

**Files:**
- Modify: `phases/11-llm-engineering/12-guardrails/docs/zh-CN.md`
- Modify: `phases/11-llm-engineering/12-guardrails/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、标题和表格**

中文化标题、frontmatter、学习目标、核心概念、工具说明、案例、交付成果、练习和术语表。保留 LLM、RAG、MCP、API、PII、DAN、LlamaGuard、NeMo Guardrails、Guardrails AI、Presidio 等必要术语。

- [x] **Step 2: 润色 Mermaid 可见标签**

把 `Guardrail Sandwich` 和 `Content Filtering Stack` 图中的可见英文标签改成中文，保持节点 ID、图方向和 Mermaid 语法不变。

- [x] **Step 3: 润色 quiz**

把 quiz 中的解释性英文短语改成自然中文，例如 `weights`、`system prompt`、`instructions`、`tokens`、`input guardrails`、`policy violations`、`red-team prompt set`、`false positive/negative rate`，保持 `correct` 索引不变。

- [x] **Step 4: 单课校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/11-llm-engineering/12-guardrails
git add phases/11-llm-engineering/12-guardrails/docs/zh-CN.md phases/11-llm-engineering/12-guardrails/quiz.zh-CN.json
git commit -m "fix(phase-11/12): polish zh-CN guardrails lesson"
```

执行记录：`python3 scripts/i18n_validate.py --lesson phases/11-llm-engineering/12-guardrails` 返回 `docs 1/1, quiz 1/1, 0 issue(s)`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 4 轮审查.md`
- Add: `cowork/plan/第 4 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：`audit_lessons.py` 返回 `503 lesson(s), 0 issue(s)`；`i18n_validate.py` 返回 `docs 503/503, quiz 338/338, 0 issue(s)`；`node --test site/tests/*.test.mjs` 返回 4 个测试全部通过。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/11-llm-engineering/12-guardrails&lang=zh-CN`。确认 H1、关键中文短语、Mermaid/代码块、代码高亮、测验文案和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：使用临时 `4177` 静态服务检查 `11/12-guardrails` 中文页。页面 H1 为 `护栏、安全与内容过滤`，出现 `护栏夹层架构`、`攻击分类`、`输入护栏`、`内容过滤栈`、`真实攻击案例` 和中文 quiz 文案；旧标题片段 `Guardrail Sandwich`、`Attack Taxonomy`、`Input Guardrails`、`Output Guardrails`、`Tools of the Trade` 未命中。页面渲染出 2 个 Mermaid SVG，9 个代码块有高亮 token。顺序运行前置定义块和演示块后，演示输出包含输入护栏、输出护栏、批量攻击模拟和统计仪表盘。浏览器 warn/error 日志为空。复验后已停止临时服务并还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 4 轮记录**

```bash
git add -f cowork/review/第\ 4\ 轮审查.md cowork/plan/第\ 4\ 轮修改计划.md
git commit -m "docs(review): add round 4 review and plan"
git push
```

执行记录：第 4 轮记录使用 `docs(review): add round 4 review and plan` 提交，并在本轮结束时推送到当前分支。
