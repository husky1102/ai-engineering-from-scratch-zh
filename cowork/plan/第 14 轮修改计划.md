# 第 14 轮 Reviewer Agent 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Reviewer Agent 课程的中文半翻译、quiz 数量不合约和代码测试缺口，并复验本地网页。

**Architecture:** 本轮只改 `phases/14-agent-engineering/39-reviewer-agent` 下的中文文档、中英 quiz、Python tests；如网页复验发现站点问题，再按实际问题最小范围修复站点。reviewer agent、builder、rubric、verification gate、handoff、LLM judge、calibration set、confidence 等必要术语保留；普通解释性英文中文化。将中英 quiz 收敛到 6 题，并调整为 1 pre、3 check、2 post。补齐 stdlib unittest 后按本课单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib demo、Python unittest、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Reviewer Agent 课程

**Files:**
- Modify: `phases/14-agent-engineering/39-reviewer-agent/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/39-reviewer-agent/quiz.json`
- Modify: `phases/14-agent-engineering/39-reviewer-agent/quiz.zh-CN.json`
- Add: `phases/14-agent-engineering/39-reviewer-agent/code/tests/`

- [x] **Step 1: 润色正文、Mermaid、rubric 和术语表**

中文化标题、导语、学习目标、Mermaid 可见标签、rubric 表格、角色分离、生产模式、实际使用、交付成果、练习、术语表和延伸阅读说明。保留必要术语和字段名。

- [x] **Step 2: 收敛并润色 quiz**

将中英 quiz 从 7 题收敛到 6 题，并调整为：
- 1 pre：为什么 builder 不能可靠地给自己的工作打分。
- 3 check：role separation、Cloudflare review architecture、LLM-judge biases。
- 2 post：calibration set、review report 与 handoff 集成。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 3: 补齐 unittest、单课校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/39-reviewer-agent
python3 phases/14-agent-engineering/39-reviewer-agent/code/main.py
cd phases/14-agent-engineering/39-reviewer-agent/code && python3 -m unittest discover tests -v
rm -f phases/14-agent-engineering/39-reviewer-agent/code/review_report_*.json
git add phases/14-agent-engineering/39-reviewer-agent/code/tests phases/14-agent-engineering/39-reviewer-agent/docs/zh-CN.md phases/14-agent-engineering/39-reviewer-agent/quiz.json phases/14-agent-engineering/39-reviewer-agent/quiz.zh-CN.json
git commit -m "fix(phase-14/39): polish reviewer agent lesson"
```

执行记录：
- `docs/zh-CN.md` 标题改为 `审查代理：将构建者与评分者分开`，Mermaid 标签、rubric 表格、生产模式、练习和术语表已中文化。
- `quiz.json` 与 `quiz.zh-CN.json` 均收敛为 6 题，阶段分布为 `pre=1/check=3/post=2`。
- 新增 7 个 unittest，覆盖 clean pass、wrong problem hard fail、forbidden scope、missing exit、soft fail boundary、handoff zero、JSON serialization。
- `python3 phases/14-agent-engineering/39-reviewer-agent/code/main.py`：通过，生成的 `review_report_*.json` 已清理。
- `cd phases/14-agent-engineering/39-reviewer-agent/code && python3 -m unittest discover tests -v`：7 tests OK。
- `python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/39-reviewer-agent`：0 issues。
- 提交：`d40cd829 fix(phase-14/39): polish reviewer agent lesson`。

### Task 2: 修复网页 runtime note 本地化

**Files:**
- Modify: `site/lesson.html`
- Modify: `site/i18n.js`
- Modify: `site/i18n/en.json`
- Modify: `site/i18n/zh-CN.json`
- Add: `site/tests/runtime_notes.test.mjs`

- [x] **Step 1: 本地化 runtime manifest 诊断 note**

为 `light Python candidate`、`Python uses packages outside the browser allowlist`、`uses API key or secret`、`reads environment variables`、`uses subprocess`、`uses network or server APIs`、`mentions GPU/CUDA`、`non-Python lesson; browser execution not enabled yet` 和 `no code files detected` 建立 i18n 映射，未知 note 保持原文。

- [x] **Step 2: 移除异步面板 loading 态**

`outputsContent` 与 `codeContent` 填充真实内容前移除 `panel-loading` class，避免运行面板正文继承全大写和宽字距。

- [x] **Step 3: 站点测试并提交**

执行记录：
- `node --test site/tests/*.test.mjs`：5 tests passed。
- `git diff --check -- site/lesson.html site/i18n.js site/i18n/en.json site/i18n/zh-CN.json site/tests/runtime_notes.test.mjs`：无输出。
- 提交：`b7d69807 fix(site): localize runtime notes`。

### Task 3: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 14 轮审查.md`
- Add: `cowork/plan/第 14 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- `python3 scripts/audit_lessons.py`：`503 lesson(s) checked, 0 issue(s)`。
- `python3 scripts/i18n_validate.py`：`docs 503/503, quiz 338/338, 0 issue(s)`。
- `node --test site/tests/*.test.mjs`：5 tests passed。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/14-agent-engineering/39-reviewer-agent&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、表格、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- 服务：`python3 -m http.server 4187 --bind 127.0.0.1`，站点根目录 `site/`。
- 页面：`http://127.0.0.1:4187/lesson.html?path=phases/14-agent-engineering/39-reviewer-agent&lang=zh-CN`。
- 浏览器 DOM：H1 为 `审查代理：将构建者与评分者分开`；`mermaidSvgs=1`；`codeBlocks=1`；正文未命中 `Problem fit|Scope discipline|Human Sign-Off|Builder Agent`。
- 运行面板：不再包含 `Python uses packages outside the browser allowlist`、`PYTHON USES PACKAGES OUTSIDE` 或 `RUNTIMENOTEOUTSIDEBROWSERALLOWLIST`；包含 `Python 使用浏览器运行器尚未启用的包。`；`codeContentHasPanelLoading=false`；runtime note 的 `textTransform=none`、`letterSpacing=normal`。
- Quiz DOM：`quiz-pre` 有 1 题，`quiz-check` 有 3 题，`quiz-post` 有 2 题。
- Quiz 交互：点击 `#pre-q0 .quiz-option[data-index="0"]` 后，`answered=true`、`disabledCount=4`、解释 `display=block`、分数 `0/1 题正确` 显示。
- 控制台：`warn/error` 日志为空。
- 截图：Browser `Page.captureScreenshot` 连续超时，记录为浏览器工具限制；DOM、控制台和交互证据已覆盖本轮复验目标。
- 生成文件：`site/data.js` 已还原，未提交。

- [ ] **Step 3: 提交并推送第 14 轮记录**

```bash
git add -f cowork/review/第\ 14\ 轮审查.md cowork/plan/第\ 14\ 轮修改计划.md
git commit -m "docs(review): add round 14 review and plan"
git push
```
