# 第 11 轮 Python 环境课程润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Python Environments 课程的中文半翻译、quiz 结构缺口和本地网页可见问题。

**Architecture:** 本轮只改 `phases/00-setup-and-tooling/06-python-environments` 下的中文文档与中英 quiz。uv、venv、conda、pip、CUDA、PyTorch、JAX、TensorFlow、lockfile、pyproject.toml 等必要术语保留；普通解释性英文、图示标签、text block 和命令注释中文化。补齐中英 quiz 到 6 题，并调整为 1 pre、3 check、2 post；完成后按本课单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Shell、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Python Environments 课程

**Files:**
- Modify: `phases/00-setup-and-tooling/06-python-environments/docs/zh-CN.md`
- Modify: `phases/00-setup-and-tooling/06-python-environments/quiz.json`
- Modify: `phases/00-setup-and-tooling/06-python-environments/quiz.zh-CN.json`

- [x] **Step 1: 润色 Mermaid、text block 和命令注释**

中文化 Mermaid 可见标签，例如 `Without virtual environments`、`System Python`、`Project A needs this`、`CONFLICT: only one torch version can exist`、`With virtual environments`。中文化目录策略 text block 和命令注释；保留实际命令、包名、路径、文件名和必要术语。

- [x] **Step 2: 润色术语表和常见错误**

把术语表调整为中文读者友好的双语映射，例如 `Lockfile（锁文件）`、`pyproject.toml`、`Transitive dependency（传递依赖）`；把 `BAD`、`GOOD`、`should show`、`These must be compatible` 等普通英文改为自然中文。

- [x] **Step 3: 补齐并重排 quiz**

将中英 quiz 从 5 题补齐到 6 题，并调整为：
- 1 pre：虚拟环境解决的问题。
- 3 check：lockfile、确认 python/pip 路径、pip/conda 混用。
- 2 post：CUDA 不匹配、按阶段环境策略。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 4: 单课校验、shell 语法校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/06-python-environments
bash -n phases/00-setup-and-tooling/06-python-environments/code/env_setup.sh
git add phases/00-setup-and-tooling/06-python-environments/docs/zh-CN.md phases/00-setup-and-tooling/06-python-environments/quiz.json phases/00-setup-and-tooling/06-python-environments/quiz.zh-CN.json
git commit -m "fix(phase-00/06): polish python environments lesson"
```

不运行 `env_setup.sh` 的安装路径；该脚本会安装课程环境包并改动本地 `.venv`，本轮只做语法校验和网页复验。

执行记录：
- `python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/06-python-environments`：docs 1/1、quiz 1/1、0 issues。
- `bash -n phases/00-setup-and-tooling/06-python-environments/code/env_setup.sh`：退出 0。
- quiz 阶段分布：`pre=1`、`check=3`、`post=2`。
- stale 文本扫描：`Without virtual environments`、`System Python`、`Project A needs this`、`CONFLICT:`、`shared lightweight env`、`BAD:`、`GOOD:`、`should show`、`These must be compatible`、`CUDA bindings` 均未残留。
- `git diff --check`：退出 0。
- 本课提交：`a290f8bc fix(phase-00/06): polish python environments lesson`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 11 轮审查.md`
- Add: `cowork/plan/第 11 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- `python3 scripts/audit_lessons.py`：503 lessons checked、0 issues。
- `python3 scripts/i18n_validate.py`：docs 503/503、quiz 338/338、0 issues。
- `node --test site/tests/*.test.mjs`：4 tests、4 pass、0 fail。
- `bash -n phases/00-setup-and-tooling/06-python-environments/code/env_setup.sh`：退出 0。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/00-setup-and-tooling/06-python-environments&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、text block、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- `node site/build.js`：生成 `site/data.js`、`sitemap.xml`、`llms.txt`，并镜像 3646 个 lesson content 文件；复验后还原不应提交的生成文件。
- 临时静态服务：`python3 -m http.server 4185 --bind 127.0.0.1`。
- Browser URL：`http://127.0.0.1:4185/lesson.html?path=phases/00-setup-and-tooling/06-python-environments&lang=zh-CN&v=round11`。
- 页面 H1：`Python 环境`。
- stale 正文检查：`Without virtual environments`、`System Python`、`Project A needs this`、`Project B needs this`、`CONFLICT: only one`、`With virtual environments`、`shared lightweight env`、`same PyTorch env`、`symlink or shared`、`might need different transformer versions`、`API SDKs, no torch needed`、`Install miniconda`、`base + PyTorch`、`everything`、`uv generates uv.lock automatically`、`pip-tools approach`、`BAD:`、`GOOD:`、`should show`、`These must be compatible`、`What people say`、`What it actually means`、`Package B depends on C` 均未在文章正文中残留。
- Mermaid：1 个 source block，1 个 `.mermaid-render svg`。
- 代码高亮：17 个 code block；TOML block 含 `.syn-*` 高亮 span，bash/text 按站点规则静态显示；17 个复制按钮。
- quiz：3 个 section，6 道题，`quiz-pre=1`、`quiz-check=3`、`quiz-post=2`，三段均可见。
- 运行代码面板：显示静态阅读中文说明；不显示 static-only 伪命令；源码卡提供 `bash env_setup.sh` 本地命令。
- 数学渲染：本课无公式，KaTeX 0，未发现 raw math hints。
- 控制台：warning/error 日志 0。

- [x] **Step 3: 提交并推送第 11 轮记录**

```bash
git add -f cowork/review/第\ 11\ 轮审查.md cowork/plan/第\ 11\ 轮修改计划.md
git commit -m "docs(review): add round 11 review and plan"
git push
```
