# 第 13 轮 Verification Gates 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Verification Gates 课程的中文半翻译、quiz 数量不合约、代码测试缺口、coverage 边界误判和本地网页可见问题。

**Architecture:** 本轮主要修改 `phases/14-agent-engineering/38-verification-gates` 下的中文文档、中英 quiz、Python demo 和本课 tests。verification gate、scope contract、feedback log、rule report、diff、verdict、reviewer、CI、override、coverage_floor、strict mode 等必要术语保留；普通解释性英文、图示标签、表格和术语表中文化。将中英 quiz 收敛到 6 题，并调整为 1 pre、3 check、2 post。执行中发现的站点问题在 `scripts/detect_runtimes.py` 与 `site/lesson.html` 中修复：runtime detector 跳过 `code/tests/`，前端隐藏 stdlib packages。完成后按本课单独提交，再提交站点修复，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib demo、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Verification Gates 课程

**Files:**
- Modify: `phases/14-agent-engineering/38-verification-gates/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/38-verification-gates/quiz.json`
- Modify: `phases/14-agent-engineering/38-verification-gates/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、Mermaid、表格和术语表**

中文化标题、导语、学习目标、Mermaid 可见标签、检查表解释、确定性 gate、override discipline、生产模式、实际使用、交付成果、练习、术语表和延伸阅读说明。保留必要术语和字段名。

- [x] **Step 2: 收敛并润色 quiz**

将中英 quiz 从 7 题收敛到 6 题，并调整为：
- 1 pre：verification gate 回答“任务是否真的完成”。
- 3 check：override discipline、Hybrid Norm pairing、defense-in-depth。
- 2 post：coverage_floor、strict mode。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 3: 补齐 unittest、修复 coverage 边界、单课校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/38-verification-gates
python3 phases/14-agent-engineering/38-verification-gates/code/main.py
cd phases/14-agent-engineering/38-verification-gates/code && python3 -m unittest discover tests -v
rm -f phases/14-agent-engineering/38-verification-gates/code/verification_report_*.json phases/14-agent-engineering/38-verification-gates/code/overrides.jsonl
git add phases/14-agent-engineering/38-verification-gates/code/main.py phases/14-agent-engineering/38-verification-gates/code/tests phases/14-agent-engineering/38-verification-gates/docs/zh-CN.md phases/14-agent-engineering/38-verification-gates/quiz.json phases/14-agent-engineering/38-verification-gates/quiz.zh-CN.json
git commit -m "fix(phase-14/38): polish verification gates lesson"
```

执行记录：
- 单课 i18n：`docs 1/1, quiz 1/1, 0 issue(s)`。
- 本课 demo：`T-001: passed=True findings=0`；`T-002` 和 `T-003` 保持 block/warn 示例。
- 本课 unittest：7 个测试通过，覆盖 acceptance、scope、coverage floor、1 个百分点 coverage 边界、strict mode 和 signed override。
- 已清理 `verification_report_*.json` / `overrides.jsonl`。
- Lesson commit：`e6e4aa52 fix(phase-14/38): polish verification gates lesson`。

### Task 1b: 修复本地网页 runtime 面板

**Files:**
- Modify: `scripts/detect_runtimes.py`
- Modify: `scripts/tests/test_detect_runtimes.py`
- Modify: `site/lesson.html`

- [x] **Step 1: 修复 runtime package 污染**

让 runtime detector 跳过 `code/tests/`，避免测试 imports 污染 runtime packages；将 `hmac` 识别为 stdlib。前端 `displayRuntimePackages` 扩展 stdlib 过滤，避免旧 manifest 中的 stdlib 包名显示为外部包。

- [x] **Step 2: 验证并提交站点修复**

执行记录：
- `python3 -m unittest scripts.tests.test_detect_runtimes -v`：2 个测试通过。
- `node --test site/tests/*.test.mjs`：4 个测试通过。
- 浏览器复验：第 38 课运行面板显示本地命令，不再显示 `包: HMAC`；Mermaid SVG 渲染 1 个；quiz 为 `pre=1/check=3/post=2`；控制台 warn/error 为 0。
- 还原不应提交的 `site/data.js` 与 `site/runtime-manifest.json`。
- Site commit：`e8e23a6f fix(site): hide stdlib runtime packages`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 13 轮审查.md`
- Add: `cowork/plan/第 13 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- `python3 scripts/audit_lessons.py`：503 lesson(s), 0 issue(s)。
- `python3 scripts/i18n_validate.py`：docs 503/503, quiz 338/338, 0 issue(s)。
- `node --test site/tests/*.test.mjs`：4/4 pass。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/14-agent-engineering/38-verification-gates&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、表格、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- URL：`http://127.0.0.1:4187/lesson.html?path=phases/14-agent-engineering/38-verification-gates&lang=zh-CN`。
- H1：`验证门`。
- Mermaid：等待渲染后 SVG count = 1。
- 代码块：命令块为 `data-code-lang="text"`，有复制按钮；本课 `text` fence 不启用 Pyodide run button。
- 运行面板：`本地运行`，命令为 `python phases/14-agent-engineering/38-verification-gates/code/main.py`，不显示 stdlib package。
- Quiz：6 题，`pre=1/check=3/post=2`；点击第一题正确选项后出现 `correct` 状态和解释文本。
- 控制台：warn/error = 0。
- 临时服务已停止，生成文件已还原。

- [ ] **Step 3: 提交并推送第 13 轮记录**

```bash
git add -f cowork/review/第\ 13\ 轮审查.md cowork/plan/第\ 13\ 轮修改计划.md
git commit -m "docs(review): add round 13 review and plan"
git push
```
