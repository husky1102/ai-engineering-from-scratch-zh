# 第 18 轮 GPU Setup 润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 GPU 配置课程的 quiz、中文翻译和 Python 可运行入口，让本地网页能直接运行轻量诊断。

**Architecture:** 保留 `gpu_check.py` 作为兼容入口，新增 `main.py` 作为课程 canonical Python 入口。`main.py` 将诊断逻辑拆成纯函数和小型 CLI；默认路径不做重型矩阵乘法，只有显式 `--benchmark` 才运行 CPU/GPU benchmark，方便网页和无 GPU 环境安全执行。

**Tech Stack:** Python stdlib `argparse`、`unittest`、可选 `torch`，Markdown、JSON、Browser DOM QA。

---

### Task 1: 为 GPU 课程补 Python 入口和测试

**Files:**
- Create: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code/main.py`
- Modify: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code/gpu_check.py`
- Create: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code/tests/test_main.py`

- [x] **Step 1: Write the failing tests**

创建 `test_main.py`，覆盖：
- `estimate_fp16_params(24_000_000_000)` 返回约 `12.0`。
- 无 PyTorch 时 `collect_gpu_info()` 返回 `torch_installed=False`、`cuda_available=False`。
- CPU-only fake torch 返回版本号且不跑 benchmark。
- CUDA fake torch 返回 GPU 名称、显存 GB、compute capability 和 fp16 估算。
- `main([])` 在无 PyTorch 时 exit 0。

- [x] **Step 2: Verify RED**

```bash
cd phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code
python3 -m unittest discover tests -v
```

Expected: fail because `main.py` or the tested functions do not exist yet.

- [x] **Step 3: Implement minimal code**

新增 `main.py`：
- 4-6 行 header comment，引用 `docs/en.md`、PyTorch CUDA API、NVIDIA SMI。
- `load_torch()` 延迟导入 torch。
- `estimate_fp16_params(total_memory_bytes)`。
- `collect_gpu_info(torch_module=None)`。
- `run_benchmark(torch_module, size)` only when requested.
- `main(argv=None)` prints friendly diagnostics and returns 0.

将 `gpu_check.py` 改为：

```python
from main import main

if __name__ == "__main__":
    raise SystemExit(main())
```

- [x] **Step 4: Verify GREEN**

```bash
cd phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code
python3 main.py
python3 -m unittest discover tests -v
```

Expected: `python3 main.py` exits 0; tests pass.

### Task 2: 润色文档和 quiz

**Files:**
- Modify: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/docs/en.md`
- Modify: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/docs/zh-CN.md`
- Modify: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/quiz.json`
- Modify: `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/quiz.zh-CN.json`

- [x] **Step 1: Update docs**

英文文档增加 `python3 main.py` 作为默认诊断命令，并说明 `--benchmark` 是可选重型路径。中文文档将概念框标签改为中文优先，Colab 步骤改为“运行时 > 更改运行时类型 > T4 GPU（Runtime > Change runtime type）”。

- [x] **Step 2: Update quiz**

将中英 quiz 改为：
- 1 pre：GPU 为什么适合神经网络训练。
- 3 check：`nvidia-smi`、VRAM、`torch.cuda.synchronize()`。
- 2 post：fp16 容量估算、何时选择 Colab/云端 GPU。

保持中英文 `stage`、选项数量和 `correct` 索引一致，并补齐 `lesson`、`title`。

### Task 3: 校验、网页复验、提交和推送

**Files:**
- Add: `cowork/review/第 18 轮审查.md`
- Add: `cowork/plan/第 18 轮修改计划.md`

- [x] **Step 1: Lesson validation**

```bash
cd phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code
python3 main.py
python3 -m unittest discover tests -v
cd -
python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/03-gpu-setup-and-cloud
```

- [x] **Step 2: Commit lesson**

```bash
git add phases/00-setup-and-tooling/03-gpu-setup-and-cloud
git commit -m "fix(phase-00/03): polish gpu setup lesson"
```

- [x] **Step 3: Refresh tracked site assets if needed**

Run `python3 scripts/detect_runtimes.py` / `python3 scripts/build_lesson_assets.py` or `node site/build.js` as needed to verify the new `main.py` appears in runtime/source metadata. Commit intentional tracked manifest updates separately as `fix(site): refresh gpu lesson assets`; restore generated `site/data.js`.

- [x] **Step 4: Full validation and Browser QA**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

Browser URL:
`http://127.0.0.1:4187/lesson.html?path=phases/00-setup-and-tooling/03-gpu-setup-and-cloud&lang=zh-CN`

Check H1, Chinese concept labels, code highlighting, runtime panel entry `main.py`, quiz distribution and one quiz interaction, and console warn/error logs.

- [ ] **Step 5: Commit and push records**

```bash
git add -f cowork/review/第\ 18\ 轮审查.md cowork/plan/第\ 18\ 轮修改计划.md
git commit -m "docs(review): add round 18 review and plan"
git push
```

## Execution Record

- RED test: `python3 -m unittest discover tests -v` failed with `ModuleNotFoundError: No module named 'main'`, confirming the missing canonical Python entry.
- Lesson commit: `8908d71c fix(phase-00/03): polish gpu setup lesson`
  - Added `code/main.py` with light default GPU diagnostics and explicit `--benchmark`.
  - Kept `code/gpu_check.py` as a compatibility wrapper.
  - Added 5 unittest cases covering missing PyTorch, CPU-only torch, CUDA details, fp16 estimate, and CLI exit 0.
  - Updated docs and quiz; both quiz files now have `pre=1/check=3/post=2`, `lesson`, and `title`.
- Site asset commit: `caf3c1c7 fix(site): refresh gpu lesson assets`
  - `site/runtime-manifest.json` now points 00/03 to `code/main.py`.
  - `site/lesson-assets.json` lists `gpu_check.py` and `main.py`, and excludes `code/tests/`.
- Browser QA follow-up commit: `0ed81aa6 fix(site): clear runtime loading subtitle`
  - Added a site test requiring the runtime panel subtitle to leave loading state after manifests resolve.
  - Updated `renderCodePanel()` so loaded panels show `本地运行` / runtime label, source files, or static reading instead of stale loading text.
- Validation:
  - `cd phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code && python3 main.py && python3 -m unittest discover tests -v`: exits 0; 5 tests OK.
  - `python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/03-gpu-setup-and-cloud`: docs 1/1, quiz 1/1, 0 issues.
  - `python3 scripts/audit_lessons.py`: 503 lessons, 0 issues.
  - `python3 scripts/i18n_validate.py`: docs 503/503, quiz 338/338, 0 issues.
  - `node --test site/tests/*.test.mjs`: 6 tests passed.
  - `git diff --check`: clean.
- Browser QA:
  - URL: `http://127.0.0.1:4187/lesson.html?path=phases/00-setup-and-tooling/03-gpu-setup-and-cloud&lang=zh-CN&qa=20260609r18b`
  - Page identity: title `GPU 配置与云端 - AI Engineering from Scratch`, H1 `GPU 配置与云端`.
  - Console health: warn/error logs empty.
  - Translation rendering: Chinese concept labels present (`你的选择`、`成本：`、`配置：`、`最适合：`), old English labels absent.
  - Code rendering: 8 code blocks, 3 Python blocks, 28 custom syntax-highlight spans.
  - Runtime/source panel: subtitle `本地运行`; entry `phases/00-setup-and-tooling/03-gpu-setup-and-cloud/code/main.py`; package `torch`; source cards include `gpu_check.py` and `main.py`; source panel does not include `code/tests/`.
  - Quiz distribution: pre 1 question, check 3 questions, post 2 questions; all questions have 4 options.
  - Quiz interaction: a real click changed quiz state, disabled 4 options, displayed the explanation, and updated the score. A first coordinate click selected a wrong option due browser coordinate offset, so the persisted wrong-answer state was not used as correctness evidence.
- Generated-file hygiene: `node site/build.js` was used for local content mirroring; generated `site/data.js` was restored before commits.
