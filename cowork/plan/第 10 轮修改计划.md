# 第 10 轮 Docker 课程润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Docker for AI 课程的中文半翻译、quiz 结构缺口和本地网页 static-only 运行面板问题。

**Architecture:** 本轮改 `phases/00-setup-and-tooling/07-docker-for-ai` 下的中文文档与中英 quiz，并修复 `site/lesson.html` 的 static-only 运行面板。Docker、Dockerfile、Docker Compose、NVIDIA Container Toolkit、CUDA、PyTorch、Qdrant、RAG、Jupyter 等必要术语保留；普通解释性英文、图示标签、text block 和命令注释中文化。补齐中英 quiz 到 6 题，并调整为 1 pre、3 check、2 post；课程修复按本课单独提交，站点修复单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Dockerfile、Docker Compose YAML、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Docker for AI 课程

**Files:**
- Modify: `phases/00-setup-and-tooling/07-docker-for-ai/docs/zh-CN.md`
- Modify: `phases/00-setup-and-tooling/07-docker-for-ai/quiz.json`
- Modify: `phases/00-setup-and-tooling/07-docker-for-ai/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、Mermaid、text block 和命令注释**

中文化 Mermaid 可见标签，例如 `Without Docker`、`Your machine`、`Their machine`、`Server`、`crashes`、`With Docker — Same image everywhere`。中文化容器模式 text block、基础镜像说明和命令注释；保留实际命令、镜像名、路径、端口、YAML key 和专有名词。

- [x] **Step 2: 润色术语表和练习**

把术语表调整为中文读者友好的双语映射，例如 `Container（容器）`、`Image layer（镜像层）`、`Volume mount（卷挂载）`、`Base image（基础镜像）`；把普通解释性英文改为自然中文。

- [x] **Step 3: 补齐并重排 quiz**

将中英 quiz 从 5 题补齐到 6 题，并调整为：
- 1 pre：容器与虚拟机的区别。
- 3 check：Dockerfile、volume mount、NVIDIA Container Toolkit。
- 2 post：Docker Compose 服务发现、base image 选择。

保持中文与英文源的 `stage`、选项数量和 `correct` 索引一致。

- [x] **Step 4: 单课校验、Docker 静态校验并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/07-docker-for-ai
docker --version
docker compose version
docker compose -f phases/00-setup-and-tooling/07-docker-for-ai/code/docker-compose.yml config
git add phases/00-setup-and-tooling/07-docker-for-ai/docs/zh-CN.md phases/00-setup-and-tooling/07-docker-for-ai/quiz.json phases/00-setup-and-tooling/07-docker-for-ai/quiz.zh-CN.json
git commit -m "fix(phase-00/07): polish docker for ai lesson"
```

如果本机没有 Docker daemon 或 Docker CLI，不构建 CUDA 镜像；记录跳过原因为 “避免下载大型 CUDA/PyTorch 镜像，且本轮只需静态验证 Dockerfile/Compose 语法”。

执行记录：
- `python3 scripts/i18n_validate.py --lesson phases/00-setup-and-tooling/07-docker-for-ai`：docs 1/1、quiz 1/1、0 issues。
- quiz 阶段分布：`pre=1`、`check=3`、`post=2`。
- `docker --version` / `docker compose version` / `docker compose -f ... config`：本机无 Docker CLI，均为 `command not found: docker`；未构建 CUDA/PyTorch 镜像。
- 替代静态校验：`ruby -e 'require "yaml"; YAML.load_file(...)'` 解析 Compose YAML 通过，服务为 `ai-dev, qdrant`。
- Dockerfile 指令检查：`FROM`、`RUN`、`WORKDIR`、`VOLUME`、`EXPOSE`、`CMD` 均存在。
- `git diff --check`：退出 0。
- 本课提交：`c8a7aa27 fix(phase-00/07): polish docker for ai lesson`。

### Task 2: 修复本地站点 static-only 运行面板

**Files:**
- Modify: `site/lesson.html`

- [x] **Step 1: 禁止未知/静态语言生成伪运行命令**

将 `langCommandFromLanguage()` 对未知语言的回退值从 `run` 改为空字符串；`static-only` runtime 不再使用 entry 拼接运行命令。

- [x] **Step 2: 使用本地化静态阅读说明**

当 manifest note 是默认的 `non-Python lesson; browser execution not enabled yet` 时，改用 `runtimeDefaultNote(runtime)`，中文页面显示 `这节课更适合作为文本或配置阅读。`。

- [x] **Step 3: 验证并提交站点修复**

```bash
node --test site/tests/*.test.mjs
git diff --check -- site/lesson.html
git add site/lesson.html
git commit -m "fix(site): avoid bogus static run commands"
```

执行记录：
- `node --test site/tests/*.test.mjs`：4 tests、4 pass、0 fail。
- `git diff --check -- site/lesson.html`：退出 0。
- Browser 复验：`run phases/00-setup-and-tooling/07-docker-for-ai/code/Dockerfile` 不再出现；中文静态说明可见；控制台 warning/error 0。
- 站点提交：`644eec11 fix(site): avoid bogus static run commands`。

### Task 3: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 10 轮审查.md`
- Add: `cowork/plan/第 10 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：
- 课程修复后：`python3 scripts/audit_lessons.py` 503 lessons checked、0 issues；`python3 scripts/i18n_validate.py` docs 503/503、quiz 338/338、0 issues；`node --test site/tests/*.test.mjs` 4 pass。
- 站点修复后重新验证：`python3 scripts/audit_lessons.py` 503 lessons checked、0 issues；`python3 scripts/i18n_validate.py` docs 503/503、quiz 338/338、0 issues；`node --test site/tests/*.test.mjs` 4 pass。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/00-setup-and-tooling/07-docker-for-ai&lang=zh-CN`。确认 H1、关键中文短语、Mermaid SVG、text block、代码高亮、运行代码面板、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：
- `node site/build.js`：生成 `site/data.js`、`sitemap.xml`、`llms.txt`，并镜像 3646 个 lesson content 文件；复验后还原不应提交的生成文件。
- 临时静态服务：`python3 -m http.server 4184 --bind 127.0.0.1`。
- Browser URL：`http://127.0.0.1:4184/lesson.html?path=phases/00-setup-and-tooling/07-docker-for-ai&lang=zh-CN&v=round10-sitefix`。
- 页面 H1：`面向 AI 的 Docker`。
- stale 正文检查：`Without Docker`、`Your machine`、`Their machine`、`Same image everywhere`、`Your code`、`Full toolkit`、`Editor support`、`Use for:`、`Size:`、`Mount your code`、`List running containers`、`Remove unused images`、`View container logs`、`What people say`、`What it actually means` 均未在文章正文中残留。
- Mermaid：1 个 source block，1 个 `.mermaid-render svg`。
- 代码高亮：18 个 code block；Dockerfile、YAML 和 Python block 包含 `.syn-*` 高亮 span；18 个复制按钮。
- quiz：3 个 section，6 道题，`quiz-pre=1`、`quiz-check=3`、`quiz-post=2`，三段均可见。
- 运行代码面板：显示静态阅读、Dockerfile 和 `docker-compose.yml` 源码入口；不再显示 `run .../Dockerfile`；中文静态说明可见。
- 数学渲染：本课无公式，KaTeX 0，未发现 raw math hints。
- 控制台：warning/error 日志 0。

- [x] **Step 3: 提交并推送第 10 轮记录**

```bash
git add -f cowork/review/第\ 10\ 轮审查.md cowork/plan/第\ 10\ 轮修改计划.md
git commit -m "docs(review): add round 10 review and plan"
git push
```
