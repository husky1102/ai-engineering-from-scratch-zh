# 第 5 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 5 轮审查发现的 MCP 生产认证课中文残留，让规范解释自然、准确、完整，同时保留标准术语、字段名和代码接口。

**Architecture:** 本轮只改 `phases/13-tools-and-protocols/18-mcp-auth-production/docs/zh-CN.md`。标准名、RFC 编号、OAuth/OIDC/MCP/CIMD/DCR/JWKS/PKCE/IdP/JWT/aud/iss/kid/client_id 等保持可识别；普通解释、表头、练习、术语表和延伸阅读说明中文化。单课代码和单课 i18n 校验通过后按课程单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、Python stdlib demo、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 MCP 生产认证课

**Files:**
- Modify: `phases/13-tools-and-protocols/18-mcp-auth-production/docs/zh-CN.md`

- [x] **Step 1: 润色标题、导语、目标和核心正文**

中文化标题、frontmatter、学习目标、问题陈述和核心概念解释。保留规范词和字段名，重点处理 `production auth`、`client population`、`enrollment`、`fallback`、`key rotation`、`audience binding`、`scheduled job`、`dispatch tool` 等解释性英文。

- [x] **Step 2: 润色表格、术语表、练习和延伸阅读**

中文化 IdP capability matrix 表头和普通值，术语表表头与说明，练习步骤和延伸阅读解释。标准标题和 RFC 名称保留原文。

- [x] **Step 3: 单课校验、运行代码并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/13-tools-and-protocols/18-mcp-auth-production
python3 phases/13-tools-and-protocols/18-mcp-auth-production/code/main.py
git add phases/13-tools-and-protocols/18-mcp-auth-production/docs/zh-CN.md
git commit -m "fix(phase-13/18): polish zh-CN mcp auth production"
```

执行记录：`python3 scripts/i18n_validate.py --lesson phases/13-tools-and-protocols/18-mcp-auth-production` 返回 `docs 1/1, quiz 0/0, 0 issue(s)`；`python3 phases/13-tools-and-protocols/18-mcp-auth-production/code/main.py` 完整跑完 discovery、DCR、audience binding、JWKS refresh 和 step-up flow。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 5 轮审查.md`
- Add: `cowork/plan/第 5 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：`audit_lessons.py` 返回 `503 lesson(s), 0 issue(s)`；`i18n_validate.py` 返回 `docs 503/503, quiz 338/338, 0 issue(s)`；`node --test site/tests/*.test.mjs` 返回 4 个测试全部通过。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/13-tools-and-protocols/18-mcp-auth-production&lang=zh-CN`。确认 H1、关键中文短语、表格、代码高亮、运行按钮和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：使用临时 `4178` 静态服务检查 `13/18-mcp-auth-production` 中文页。页面 H1 为 `生产环境中的 MCP 认证：接入注册、JWKS 刷新与受众固定令牌`，出现 `IdP 能力矩阵`、`JWKS 刷新模式`、`验证例程`、`失败模式`，旧栏目名 `IdP capability matrix`、`Key Terms`、`Further Reading`、`Use It`、`Ship It` 未命中。页面渲染 2 张表、7 个代码块，并有代码高亮 token。该课没有 quiz 文件，页面对 quiz 的 404 探测符合当前课程结构；浏览器 warn/error 日志为空。复验后已停止临时服务并还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 5 轮记录**

```bash
git add -f cowork/review/第\ 5\ 轮审查.md cowork/plan/第\ 5\ 轮修改计划.md
git commit -m "docs(review): add round 5 review and plan"
git push
```

执行记录：第 5 轮记录使用 `docs(review): add round 5 review and plan` 提交，并在本轮结束时推送到当前分支。
