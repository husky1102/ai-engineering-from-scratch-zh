# 第 1 轮修改计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 修复第 1 轮审查发现的本地网页关键路径问题，让 `site/` 静态根入口能加载中文课程、测验和代码/输出资源，并去掉中文页面中的重复英文通用测验。

**Architecture:** 构建阶段把课程源文件复制到 `site/content/`，运行时优先读 `content/...`，再回退到仓库根入口使用的 `../phases/...`。Lesson 页面继续用现有原生 JS 渲染 Markdown、quiz、Pyodide 和 AI 面板，只修补资源路径、本地化和纯文本高亮策略。

**Tech Stack:** Node.js stdlib build script、Python stdlib manifest script、vanilla HTML/CSS/JS、现有 Browser/Pyodide runner。

---

### Task 1: 构建站点内容镜像

**Files:**
- Modify: `site/build.js`
- Modify: `.gitignore`

- [x] **Step 1: 在 `.gitignore` 忽略生成内容目录**

在生成文件规则附近加入：

```gitignore
site/content/
```

- [x] **Step 2: 在 `site/build.js` 增加内容镜像常量和 helper**

在顶部路径常量附近加入：

```javascript
const CONTENT_ROOT = path.join(__dirname, 'content');
const PHASES_ROOT = path.join(REPO_ROOT, 'phases');
const CONTENT_IGNORED_DIRS = new Set(['__pycache__', '.ipynb_checkpoints']);
const CONTENT_IGNORED_FILES = new Set(['.DS_Store']);
```

新增 helper：

```javascript
function resetDir(dir) {
  fs.rmSync(dir, { recursive: true, force: true });
  fs.mkdirSync(dir, { recursive: true });
}

function copyTree(src, dest) {
  if (!fs.existsSync(src)) return 0;
  const stat = fs.statSync(src);
  if (stat.isDirectory()) {
    if (CONTENT_IGNORED_DIRS.has(path.basename(src))) return 0;
    fs.mkdirSync(dest, { recursive: true });
    let copied = 0;
    for (const entry of fs.readdirSync(src)) {
      copied += copyTree(path.join(src, entry), path.join(dest, entry));
    }
    return copied;
  }
  if (!stat.isFile()) return 0;
  if (CONTENT_IGNORED_FILES.has(path.basename(src))) return 0;
  fs.mkdirSync(path.dirname(dest), { recursive: true });
  fs.copyFileSync(src, dest);
  return 1;
}
```

- [x] **Step 3: 生成 `site/content/phases`**

新增：

```javascript
function writeContentMirror() {
  resetDir(CONTENT_ROOT);
  const target = path.join(CONTENT_ROOT, 'phases');
  const copied = copyTree(PHASES_ROOT, target);
  console.log(`   mirrored lesson content (${copied} files)`);
}
```

在 `build()` 的末尾生成 sitemap/llms 附近调用：

```javascript
writeContentMirror();
```

### Task 2: 让 lesson 页面优先读 `site/content`

**Files:**
- Modify: `site/lesson.html`

- [x] **Step 1: 新增内容 URL helper**

在 `lessonHref()` 附近新增：

```javascript
function lessonContentUrl(path, suffix) {
  return 'content/' + path + '/' + suffix;
}

function lessonRepoUrl(path, suffix) {
  return '../' + path + '/' + suffix;
}
```

- [x] **Step 2: 修改 doc/quiz 候选路径**

将 `lessonDocCandidates(path)` 改为：

```javascript
function lessonDocCandidates(path) {
  if (currentLang === 'en') {
    return [
      { url: lessonContentUrl(path, 'docs/en.md'), lang: 'en' },
      { url: lessonRepoUrl(path, 'docs/en.md'), lang: 'en' }
    ];
  }
  return [
    { url: lessonContentUrl(path, 'docs/zh-CN.md'), lang: 'zh-CN' },
    { url: lessonContentUrl(path, 'docs/en.md'), lang: 'en' },
    { url: lessonRepoUrl(path, 'docs/zh-CN.md'), lang: 'zh-CN' },
    { url: lessonRepoUrl(path, 'docs/en.md'), lang: 'en' }
  ];
}
```

将 `lessonQuizCandidates(path)` 同理改成优先 `content/.../quiz.zh-CN.json` 和 `content/.../quiz.json`，再回退 `../...`。

### Task 3: 修复资源卡片 URL

**Files:**
- Modify: `scripts/build_lesson_assets.py`
- Regenerate (tracked): `site/lesson-assets.json`

- [x] **Step 1: 修改 `local_url()`**

将：

```python
return "../" + rel(path)
```

改为：

```python
return "content/" + rel(path)
```

- [x] **Step 2: 重建 lesson assets**

运行：

```bash
python3 scripts/build_lesson_assets.py
```

预期：

```text
build_lesson_assets.py — 503 lesson(s), 796 code file(s), 518 output file(s)
```

### Task 4: 移除重复英文通用测验并本地化 AI 面板

**Files:**
- Modify: `site/lesson.html`
- Modify: `site/i18n.js`
- Modify: `site/i18n/en.json`
- Modify: `site/i18n/zh-CN.json`

- [x] **Step 1: 停止渲染泛化 quiz panel**

在 `renderAIPanels(container)` 中删除：

```javascript
renderQuizPanel(container);
```

保留 `renderQuiz()` 从 `quiz.json` / `quiz.zh-CN.json` 注入的课内测验。

- [x] **Step 2: 为学习路径与继续学习加入 i18n key**

新增 key：

```json
{
  "learningPathTitle": "Learning Path",
  "phaseLabel": "Phase",
  "earlierLessons": "earlier lessons",
  "laterLessons": "later lessons",
  "phaseProgress": "You've completed {completed} of {total} lessons in this phase",
  "readyForPhase": "Ready for Phase {phase}: {name}",
  "continueLearningTitle": "Continue Learning",
  "phaseFinished": "You finished this phase!",
  "browsePhaseLessons": "Browse all Phase {phase} lessons",
  "fullCourseCatalog": "Full course catalog",
  "personalPathCallout": "Run /find-your-level in Claude, Cursor, Codex, OpenClaw, Hermes, or any agent with the curriculum skills installed for a personalized learning path"
}
```

中文对应：

```json
{
  "learningPathTitle": "学习路径",
  "phaseLabel": "阶段",
  "earlierLessons": "前面的课程",
  "laterLessons": "后面的课程",
  "phaseProgress": "你已完成本阶段 {total} 课中的 {completed} 课",
  "readyForPhase": "已准备进入第 {phase} 阶段：{name}",
  "continueLearningTitle": "继续学习",
  "phaseFinished": "你已完成本阶段！",
  "browsePhaseLessons": "浏览第 {phase} 阶段全部课程",
  "fullCourseCatalog": "完整课程目录",
  "personalPathCallout": "在 Claude、Cursor、Codex、OpenClaw、Hermes 或任何安装了课程技能的智能体中运行 /find-your-level，生成个性化学习路径"
}
```

同步把这些 key 加入 `site/i18n.js` 的 fallback 字典。

- [x] **Step 3: 替换硬编码英文**

在 `renderLearningPathPanel()` 和 `renderContinuePanel()` 中用 `t(key, values)` 替换：
- `Learning Path`
- `Phase`
- `earlier lessons`
- `later lessons`
- `You've completed ...`
- `Ready for Phase ...`
- `Continue Learning`
- `You finished this phase!`
- `Browse all Phase ... lessons`
- `Full course catalog`
- `/find-your-level` callout 的英文句子。

### Task 5: 纯文本块不做代码高亮

**Files:**
- Modify: `site/lesson.html`

- [x] **Step 1: 在 `highlightSyntax()` 开头跳过文本类语言**

加入：

```javascript
if (!lang || lang === 'text' || lang === 'console' || lang === 'bash' || lang === 'shell' || lang === 'mermaid') {
  return code;
}
```

保留 Python、Julia、TypeScript、Rust 等语言高亮。

### Task 6: 验证

**Files:**
- No code edits expected.

- [x] **Step 1: 运行结构与站点测试**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
python3 scripts/build_lesson_assets.py
node site/build.js
```

预期：
- audit 0 issue。
- i18n validate 0 issue。
- Node tests 4 pass。
- lesson assets 生成成功。
- build 生成成功，并写出 `site/content/`。

- [x] **Step 2: 浏览器验证 `site/` 静态根入口**

启动：

```bash
cd site
python3 -m http.server 4173 --bind 127.0.0.1
```

打开：

```text
http://127.0.0.1:4173/lesson.html?path=phases/01-math-foundations/01-linear-algebra-intuition&lang=zh-CN
```

检查：
- 页面不是 `未找到课程`。
- H1 是 `线性代数直觉`。
- 只出现中文 lesson quiz，不出现 `QUESTION 1 OF 3` 泛化测验。
- AI 面板标题为中文。
- Python 代码块仍有高亮。
- `text` 公式块没有 `.syn-*` 高亮 span。
- Pyodide 第一段 Python 能运行并输出向量结果。

- [x] **Step 3: 保持生成文件边界**

提交前确认：
- 不提交 `site/data.js` 的 timestamp-only diff。
- 不提交 `site/content/`。
- 提交 `site/lesson-assets.json`，因为它是当前仓库已跟踪的站点 manifest。

### Task 7: 修复 Notebook 站点内链接

**Files:**
- Modify: `scripts/build_notebooks.py`
- Regenerate (tracked): `site/runtime-manifest.json`

- [x] **Step 1: 新增 notebook URL helper**

在 `scripts/build_notebooks.py` 中新增：

```python
def notebook_url_for(notebook_path: str) -> str:
    """Return the site-local notebook file URL for a generated notebook path."""

    if notebook_path.startswith("site/"):
        return notebook_path[len("site/") :]
    return notebook_path
```

- [x] **Step 2: 写回 manifest 时使用站点内 URL**

将：

```python
record["notebook"] = notebook_rel
```

改为：

```python
record["notebook"] = notebook_url_for(notebook_rel)
```

- [x] **Step 3: 重建 notebooks manifest**

运行：

```bash
python3 scripts/build_notebooks.py
```

预期：

```text
build_notebooks.py — wrote 20 notebook(s)
```
