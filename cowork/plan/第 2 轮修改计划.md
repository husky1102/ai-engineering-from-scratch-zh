# 第 2 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 2 轮审查发现的三门课程中文残留和半翻译问题，同时保留站点第 1 轮修复已通过的行为。

**Architecture:** 本轮只改课程自己的 `docs/zh-CN.md` 和必要的 `quiz.zh-CN.json`，不修改英文源、不生成缺失 quiz、不改站点架构。每门课单独验证、单独提交；最后提交本轮 `cowork/review/` 与 `cowork/plan/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色线性代数直觉课

**Files:**
- Modify: `phases/01-math-foundations/01-linear-algebra-intuition/docs/zh-CN.md`

- [x] **Step 1: 翻译 Mermaid 和说明块的可见英文标签**

将矩阵变换图中的 `Before`、`After`、`Matrix Multiplication`、`Point A`、`M (transformation)` 等改为中文；将点积说明块中的 `Same direction`、`Perpendicular`、`Opposite direction` 改为中文；将投影图中的 `Projection of a onto b`、`Origin`、`projection`、`residual (perpendicular)` 改为中文；将 Gram-Schmidt 说明块的 `Input` / `Output` 注释改为中文。

- [x] **Step 2: 校验本课翻译契约**

Run:

```bash
python3 scripts/i18n_validate.py --lesson phases/01-math-foundations/01-linear-algebra-intuition
```

Expected: lesson validation exits 0.

- [x] **Step 3: 单课提交**

```bash
git add phases/01-math-foundations/01-linear-algebra-intuition/docs/zh-CN.md
git commit -m "fix(phase-01/01): polish zh-CN linear algebra lesson"
```

### Task 2: 润色音频语言模型课

**Files:**
- Modify: `phases/06-speech-and-audio/10-audio-language-models/docs/zh-CN.md`

- [x] **Step 1: 中文化非代码、非专有名词的可见英文**

修正 `~45 minutes`、四类任务标题和示例问题、模型表/benchmark 表表头、常见陷阱、交付成果、练习难度和术语表释义。保留 Qwen、Audio Flamingo、MMAU-Pro、LongAudioBench、代码字符串和必要英文术语对照。

- [x] **Step 2: 校验本课翻译契约**

Run:

```bash
python3 scripts/i18n_validate.py --lesson phases/06-speech-and-audio/10-audio-language-models
```

Expected: lesson validation exits 0. 本课没有源 `quiz.json`，不创建 `quiz.zh-CN.json`。

- [x] **Step 3: 单课提交**

```bash
git add phases/06-speech-and-audio/10-audio-language-models/docs/zh-CN.md
git commit -m "fix(phase-06/10): polish zh-CN audio language models"
```

### Task 3: 润色编排模式课与 quiz

**Files:**
- Modify: `phases/14-agent-engineering/28-orchestration-patterns/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/28-orchestration-patterns/quiz.zh-CN.json`

- [x] **Step 1: 润色正文与命令块**

统一 frontmatter 全角冒号；翻译 Anthropic 引文并保留文章名/术语；把解释性短语（如 `loop back to self`、`topology choice`、`event-driven automation`）中文化。执行时发现 `scripts/i18n_validate.py` 要求中英同位置代码围栏语言标签一致，因此运行命令块保持英文源对应的 `text`。

- [x] **Step 2: 润色 quiz**

把 quiz 里可自然中文化的短语改成中文表达，例如 `tool surface`、`context-engineering control`、`transfers`、`test fixture`，同时保持 `correct` 索引不变。

- [x] **Step 3: 校验本课翻译契约**

Run:

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/28-orchestration-patterns
```

Expected: lesson validation exits 0.

- [x] **Step 4: 单课提交**

```bash
git add phases/14-agent-engineering/28-orchestration-patterns/docs/zh-CN.md phases/14-agent-engineering/28-orchestration-patterns/quiz.zh-CN.json
git commit -m "fix(phase-14/28): polish zh-CN orchestration patterns"
```

### Task 4: 全量验证与本轮记录提交

**Files:**
- Add: `cowork/review/第 2 轮审查.md`
- Add: `cowork/plan/第 2 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

Expected: all commands exit 0.

- [x] **Step 2: 浏览器复验**

打开：

```text
http://127.0.0.1:4173/lesson.html?path=phases/01-math-foundations/01-linear-algebra-intuition&lang=zh-CN
```

确认中文课程仍可加载，quiz 只有一套，Python 高亮存在，`text` 公式块未被高亮，运行按钮仍输出结果。

执行记录：本地 `site/` 静态根需要先运行 `node site/build.js` 刷新 `site/content/` 镜像；复验完成后已还原不应提交的生成文件 `site/data.js`。浏览器复验覆盖三门本轮润色课程，控制台 warn/error 为空；`06-speech-and-audio/10-audio-language-models` 没有源 quiz，quiz 404 回退是预期行为。

- [x] **Step 3: 提交第 2 轮记录**

```bash
git add -f cowork/review/第\ 2\ 轮审查.md cowork/plan/第\ 2\ 轮修改计划.md
git commit -m "docs(review): add round 2 review and plan"
git push
```
