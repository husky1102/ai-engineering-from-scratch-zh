# 第 6 轮翻译润色实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第 6 轮审查发现的 Agent 工作台课程中文残留，让正文、表格、图示和 quiz 更自然、准确、完整。

**Architecture:** 本轮只改 `phases/14-agent-engineering/31-agent-workbench-why-models-fail` 下的中文文档和中文 quiz。工作台七表面、八个分布式原语和 vendor pattern 的中文解释要统一；代码、命令、产品名、文章标题和文件名保持可识别。单课校验和代码运行后按课程单独提交，再做全量验证、本地网页复验，最后提交本轮 `cowork/` 记录并推送。

**Tech Stack:** Markdown、JSON、Python stdlib demo、Python 校验脚本、Node.js 站点测试、Browser DOM 复验。

---

### Task 1: 润色 Agent 工作台课程

**Files:**
- Modify: `phases/14-agent-engineering/31-agent-workbench-why-models-fail/docs/zh-CN.md`
- Modify: `phases/14-agent-engineering/31-agent-workbench-why-models-fail/quiz.zh-CN.json`

- [x] **Step 1: 润色正文、表格和 Mermaid**

中文化导语、目标、工作台七表面表格、Mermaid 可见标签、原语表、模式翻译表、证据/批评段落和术语表。保留 Claude Code、Codex、LangGraph、MCP、Terminal Bench、Vercel、Harvey 等专有名词。

- [x] **Step 2: 润色 quiz**

把 quiz 中的解释性英文短语改为中文，例如 `surfaces`、`harness`、`success rate`、`top 30`、`system of record`、`vocabulary`，保持 `correct` 索引不变。

- [x] **Step 3: 单课校验、运行代码并提交**

```bash
python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/31-agent-workbench-why-models-fail
python3 phases/14-agent-engineering/31-agent-workbench-why-models-fail/code/main.py
git add phases/14-agent-engineering/31-agent-workbench-why-models-fail/docs/zh-CN.md phases/14-agent-engineering/31-agent-workbench-why-models-fail/quiz.zh-CN.json
git commit -m "fix(phase-14/31): polish zh-CN agent workbench lesson"
```

执行记录：`python3 scripts/i18n_validate.py --lesson phases/14-agent-engineering/31-agent-workbench-why-models-fail` 返回 `docs 1/1, quiz 1/1, 0 issue(s)`；`python3 phases/14-agent-engineering/31-agent-workbench-why-models-fail/code/main.py` 跑完 prompt-only 与 workbench 对比，并已删除运行生成的临时 `outputs/failure_modes.json`。

### Task 2: 全量验证、本地复验与提交记录

**Files:**
- Add: `cowork/review/第 6 轮审查.md`
- Add: `cowork/plan/第 6 轮修改计划.md`

- [x] **Step 1: 全量自动校验**

```bash
python3 scripts/audit_lessons.py
python3 scripts/i18n_validate.py
node --test site/tests/*.test.mjs
```

执行记录：`audit_lessons.py` 返回 `503 lesson(s), 0 issue(s)`；`i18n_validate.py` 返回 `docs 503/503, quiz 338/338, 0 issue(s)`；`node --test site/tests/*.test.mjs` 返回 4 个测试全部通过。

- [x] **Step 2: 本地站点复验**

运行 `node site/build.js` 刷新 `site/content/` 镜像，使用临时静态服务检查 `lesson.html?path=phases/14-agent-engineering/31-agent-workbench-why-models-fail&lang=zh-CN`。确认 H1、关键中文短语、Mermaid、表格、代码高亮、运行按钮、quiz 和控制台日志正常；复验后还原不应提交的生成文件。

执行记录：`node site/build.js` 后用临时服务打开第 6 轮课程页，控制台 `warn/error` 为 0；正文 H1、4 个表格、1 个 Mermaid SVG、quiz 均正常。正文区域包含 `范围契约`、`Repo 记忆`、`运行时反馈`、`验证闸门`，且不再包含 `Scope Contract`、`Repo Memory`、`Runtime Feedback`、`Verification Gate`。首次全页扫描命中的英文来自左侧导航的相邻课程标题，不属于本课正文残留。本页唯一代码块是 `text` 命令 `python3 code/main.py`，无网页运行按钮和语法 token 属预期；课程 Python demo 已在单课步骤中运行通过。复验后已停止临时服务并还原 `site/data.js`。

- [x] **Step 3: 提交并推送第 6 轮记录**

```bash
git add -f cowork/review/第\ 6\ 轮审查.md cowork/plan/第\ 6\ 轮修改计划.md
git commit -m "docs(review): add round 6 review and plan"
git push
```

执行记录：计划文件更新后提交并推送本轮记录。
