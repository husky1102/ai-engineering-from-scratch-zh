# 第 19 轮审查

日期：2026-06-09

范围：
- 记录浏览器批注中的人工标注。
- 本轮先只收集问题，不修改代码、不制定修复计划。

## 浏览器批注记录

### 1. 主题切换按钮应改为日月图标

页面：
- `http://127.0.0.1:8765/lesson.html?path=phases/08-generative-ai/02-autoencoders-vae`

目标：
- `button#themeToggle`
- 可访问名称：`Toggle theme`
- 位置：`header > div > button`

标注：
- 当前主题切换按钮显示为文字/缩写形态，应改成日月图标。

### 2. Fork 后不应保留原项目 GitHub star 区块

页面：
- `http://127.0.0.1:8765/lesson.html?path=phases/08-generative-ai/02-autoencoders-vae`

目标：
- `header.site-header > div.header-inner > nav.header-nav > a.header-github:nth-of-type(5)`
- 可见文本：`30.4K`
- 位置：`header > div > nav > a`

标注：
- 当前 fork 项目不需要保留原项目的 GitHub star/count 展示区块。

### 3. 中文页面顶部导航栏也需要翻译

页面：
- `http://127.0.0.1:8765/lesson.html?path=phases/08-generative-ai/02-autoencoders-vae`

目标：
- `html > body > header.site-header > div.header-inner`
- 可见文本：`AI / FROM SCRATCH CONTENTS CATALOG ROADMAP GLOSSARY 30.4K 中文 EN N`
- 位置：`header > div`

标注：
- 当前中文页面顶部导航仍显示英文导航项，例如 `CONTENTS`、`CATALOG`、`ROADMAP`、`GLOSSARY`，需要翻译成中文。

### 4. 中文页面课程侧边栏也需要翻译

页面：
- `http://127.0.0.1:8765/lesson.html?path=phases/08-generative-ai/02-autoencoders-vae`

目标：
- `aside#lessonSidebar`
- 可见文本片段：`← PHASE 07: TRANSFORMERS DEEP DIVE PHASE 09: REINFORCEMENT LEARNING → Phase 08 ·`
- 位置：`div > aside`

标注：
- 当前中文页面侧边栏仍显示英文 phase 导航和课程标题，需要翻译成中文。

### 5. 移除网页内直接运行代码功能

页面：
- `http://127.0.0.1:8765/lesson.html?path=phases/08-generative-ai/02-autoencoders-vae`

目标：
- `div#codeContent > div.code-cards > div.code-card:nth-of-type(1)`
- 可见文本片段：`浏览器可运行 PYTHON PY phases/08-generative-ai/02-autoencoders-vae/code/main.py`
- 位置：`div > div > div > div`

标注：
- 网页内直接运行代码的功能应移除，保持和原版一样。

### 6. 主页需要中文化并移除原仓库信息

页面：
- `http://127.0.0.1:8765/index.html`

目标：
- 主页整体内容和顶部导航/hero 区域。
- 可见文本片段：`AI ENGINEERING FROM SCRATCH`、`503 lessons. 20 phases.`、`Maintained by Rohit Ghumare and contributors.`、`Star on GitHub`、`Follow @rohitg00`、`30.4K`

标注：
- 主页也需要翻译成中文。
- 移除原仓库相关信息，包括原作者维护文案、GitHub star/count、follow 原作者等内容。
- 只保留一个跳转到原仓库的按钮即可。

### 7. 主页 contents 弹窗需要中文化并改成本地课程跳转

页面：
- `http://127.0.0.1:8765/index.html#contents`

目标：
- `div#modal`
- 可见文本片段：`PHASE 03 DEEP LEARNING CORE Neural networks from first principles. No frameworks until you build one.`
- 位置：`main > div > div`

标注：
- contents/phase 弹窗内容需要翻译成中文。
- 弹窗内点击课程时应跳转本地课程页面，而不是远程 GitHub 仓库页面。

### 8. 主页相关部分需要系统整理和优化

页面：
- `http://127.0.0.1:8765/index.html`

目标：
- 主页整体，包括顶部导航、hero、contents、phase 弹窗、页脚和原仓库相关入口。

标注：
- 主页相关部分整体没有完成优化，需要做一次系统整理。
- 整理范围应覆盖中文化、去除原仓库强绑定信息、保留单一原仓库跳转按钮、本地课程跳转、以及前面几条主页/导航标注的合并修复。

## 实施记录

日期：2026-06-09

已处理：
- 首页、共享导航和页脚改为中文站点框架，移除 GitHub star/count/follow/report chrome。
- 首页只保留一个 `查看原仓库` 按钮；目录和课程入口均走本地页面。
- contents 弹窗使用中文 phase 名称/描述和中文课程标题，并跳转 `lesson.html?path=...&lang=zh-CN`。
- `site/build.js` 增加 `nameZh` / `descZh` 数据，lesson 标题从 `docs/zh-CN.md` H1 读取。
- `lesson.html` 侧边栏优先显示中文 phase/lesson 名称。
- 课程页不再加载 Pyodide runner，不再展示 `浏览器可运行` / `browser-pyodide` 作为用户可见能力。
- 主题按钮改为日/月图标，并随状态更新可访问名称。

验证：
- `node --test site/tests/round19_review.test.mjs`
- `node --test site/tests/round19_review.test.mjs site/tests/runtime_notes.test.mjs site/tests/cmdpalette_filters.test.mjs`
- `node --test site/tests/*.test.mjs`
- `node site/build.js`
- `python3 scripts/audit_lessons.py`

结果：
- Node 站点测试：15 个测试通过。
- 课程审计：503 lesson(s) checked, 0 issue(s)。
- 本地生成的 `site/data.js` 已确认包含 `nameZh` / `descZh`；按仓库规则生成文件不应作为源码提交。
