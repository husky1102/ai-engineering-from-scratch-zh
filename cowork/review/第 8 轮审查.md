# 第 8 轮审查

日期：2026-06-09

范围：
- 复查第 7 轮后的翻译残留。
- 优先处理 LLM 从零实现阶段中会影响网页阅读的半翻译课程。
- 继续检查本地网页渲染、代码高亮、可运行代码和控制台日志。

## 结论

第 8 轮仍有问题，不能结束循环。

`phases/10-llms-from-scratch/12-inference-optimization` 是当前最明确的问题课程。它的标题、导语、目标、正文、Mermaid 图、表格、动手步骤和 quiz 都保留大量普通英文。课程主题是推理优化，概念本身已经密集；半英半中会让读者更难分清 prefill/decode、KV cache、continuous batching、PagedAttention 和 speculative decoding 的关系。

## Findings

### P1 - Inference Optimization 课程正文、图示、表格和 quiz 半翻译

文件：
- `phases/10-llms-from-scratch/12-inference-optimization/docs/zh-CN.md`
- `phases/10-llms-from-scratch/12-inference-optimization/quiz.zh-CN.json`

证据：
- 标题和导语保留 `Inference Optimization`、`inference`、`prompt`、`compute-bound`、`memory-bound`。
- 学习目标和问题段保留 `autoregressive token generation`、`GPU utilization`、`throughput/latency tradeoffs`、`tokens per second`、`endpoint`、`batch slot`、`scaling problem`、`scheduling problem`。
- Mermaid 图中大量可见标签仍为英文，例如 `All prompt tokens`、`Parallel attention`、`Without KV Cache`、`Static batching`、`Wasting 40 steps`、`Contiguous allocation`、`Draft model`、`Verify all 5 in one pass`。
- 表格列名和单元格保留 `Method`、`Draft source`、`Acceptance rate`、`Overhead`、`Engine`、`Key innovation`、`Best for`、`Metric`、`Scenario`、`Bound`、`Optimize with`。
- 动手实现步骤标题保留 `Attention with KV Cache`、`Continuous Batching Simulator`、`Prefix Cache`、`Speculative Decoding Simulator`。
- quiz 中保留 `Training and evaluation`、`prompt tokens`、`autoregressive`、`model weights`、`memory bandwidth`、`generation step`、`previous tokens`、`virtual memory`、`fixed-size blocks`、`candidate tokens` 等可中文化内容。

影响：
- 本课解释生产推理服务的成本和延迟瓶颈，是后续 serving、batch API、SRE 与 GPU 调度课程的基础；半翻译会直接影响概念吸收和页面可读性。

### P2 - 本课中文页面语言字段未反映 Rust 入口

文件：
- `phases/10-llms-from-scratch/12-inference-optimization/docs/zh-CN.md`

证据：
- `code/` 中存在 `main.py` 和 `main.rs`，但中文文档语言字段只写 Python。

影响：
- 本轮先修复中文页面可见元数据，并在验证中同时运行 Python 与 Rust demo；不扩大到 README/ROADMAP 全局课程表。

## 第 8 轮建议修复范围

1. 润色 `docs/zh-CN.md` 的标题、导语、学习目标、正文、Mermaid 标签、表格列名、动手步骤、交付成果、练习、术语表和延伸阅读说明。
2. 润色 `quiz.zh-CN.json` 的问题、选项和解释，保持原有数组结构和 `correct` 索引不变。
3. 保留模型名、引擎名、算法名、指标缩写、公式、代码、命令和常用术语，例如 Prefill、Decode、KV cache、PagedAttention、EAGLE、TensorRT-LLM、ops:byte、TPS。
4. 运行单课 i18n 校验、Python demo、Rust demo、全量验证和本地网页复验；翻译提交按本课单独提交。
