# 第 3 轮审查

日期：2026-06-09

范围：
- 复查第 2 轮后的翻译残留。
- 抽样检查站点页面是否仍能加载、运行、渲染公式/图示和代码高亮。
- 继续区分“必要英文术语”与“应该自然翻译的解释性英文”。

## 结论

第 3 轮仍有问题，不能结束循环。

第 2 轮修复和本地站点复验已经通过：三门已润色课程能从 `site/` 静态根加载，第一门课的公式说明块和 Mermaid 源文本已中文化，Python 高亮和运行按钮正常，浏览器 warn/error 日志为空。

新的残留集中在后续阶段课程：部分课程标题、frontmatter、表格、Mermaid 标签、quiz 选项和正文解释仍保留大量英文短语。它们超过了“保留术语”的范围，会让中文课程读起来像半翻译。

## 已通过项

- `python3 scripts/audit_lessons.py`
  - 503 lesson(s) checked, 0 issue(s)
- `python3 scripts/i18n_validate.py`
  - docs 503/503, quiz 338/338, 0 issue(s)
- `node --test site/tests/*.test.mjs`
  - 4 tests passed.
- 浏览器复验：
  - `01-linear-algebra-intuition`：中文 H1、中文 quiz、无底部英文通用 quiz、Python 高亮、`text` 公式块无语法染色、第一个 Python 运行输出正常。
  - `06-audio-language-models`：H1、`~45 分钟`、`转写 / 语义推理 / 长音频检索` 已进入本地页面。
  - `14/28-orchestration-patterns`：中文 H1、Anthropic 引文中文、quiz 文案中文化。
  - 控制台 warn/error 日志为空。

## Findings

### P1 - 生产级 LLM 应用课仍是半翻译状态

文件：
- `phases/11-llm-engineering/13-production-app/docs/zh-CN.md`
- `phases/11-llm-engineering/13-production-app/quiz.zh-CN.json`

证据：
- frontmatter 仍有 `Build (Capstone)`、`Phase 11 Lessons 01-15`、`~120 minutes`。
- 开头和目标保留大量解释性英文：`prompts`、`RAG pipelines`、`function calling`、`caching layers`、`guardrails`、`production-ready service`。
- 表格标题和列名保留 `Component`、`Technology`、`Purpose`、`Protocol`、`Latency`、`Complexity`、`Metric`、`Target`、`Why`、`Item`、`Category`。
- Mermaid 图中的可见标签仍是英文：`Client`、`API Gateway`、`Semantic Cache`、`Cost Tracker`、`Response`。
- quiz 选项和解释保留 `Infrastructure`、`error handling`、`streaming`、`timeouts`、`provider outages`、`graceful degradation` 等可自然中文化短语。

影响：
- 这门课是 Phase 11 capstone，用户阅读时会在正文、图示、表格和 quiz 中反复遇到半英文解释，影响自然度和完整性。

### P1 - 通信协议课的标题、目标、图示和 quiz 未充分中文化

文件：
- `phases/16-multi-agent-and-swarms/03-communication-protocols/docs/zh-CN.md`
- `phases/16-multi-agent-and-swarms/03-communication-protocols/quiz.zh-CN.json`

证据：
- 标题仍为 `Communication Protocols`。
- 学习目标与问题陈述保留 `MCP tool discovery`、`invocation`、`servers`、`tools`、`protocols`、`agents`、`strings`、`deadlock`、`contract`。
- Mermaid 图示标签大量英文：`How do agents trust strangers?`、`Agent Cards`、`task lifecycle`、`Tool discovery`。
- 表格列名和状态说明保留 `State`、`Terminal?`、`Meaning`、`Blocking`、`Response`、`complete result`。
- quiz 选项和解释保留 `training data`、`external servers`、`hardcode`、`task delegation`、`visual dashboard`、`security credential`、`type safety`、`interoperability`。

影响：
- 本课解释协议边界，术语必须准确；但大量解释性英文会削弱中文学习者对协议差异的理解。

### P2 - MCP 生产认证课属于下一轮候选

文件：
- `phases/13-tools-and-protocols/18-mcp-auth-production/docs/zh-CN.md`

证据：
- 标题和开头混合 `Enrollment`、`JWKS Refresh`、`Audience-Pinned Tokens`、`client enrollment`、`dynamic client registration`。
- 但该课引用 RFC、MCP spec、OAuth/OIDC 标准名较多，英文术语有较高保留必要性。

影响：
- 应单独审查，避免把规范术语误翻成不准确中文。

## 第 3 轮建议修复范围

1. 润色 `11-llm-engineering/13-production-app` 的正文、表格、Mermaid 标签、步骤标题和 quiz；代码块中的 API 名、类名、prompt 字符串和模拟响应不做大规模改写。
2. 润色 `16-multi-agent-and-swarms/03-communication-protocols` 的标题、frontmatter、目标、正文解释、Mermaid 标签、表格列名和 quiz；协议名、JSON 字段、状态枚举、代码类型名保持准确。
3. 每课独立校验并独立提交。
4. 本轮不处理 `13/18-mcp-auth-production`，放入后续轮次。
