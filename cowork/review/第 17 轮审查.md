# 第 17 轮审查

日期：2026-06-09

范围：
- 接续第 16 轮后，检查自动扫描出的下一课：`00-setup-and-tooling/02-git-and-collaboration`。
- 重点检查中文翻译自然度、quiz 结构和本地网页渲染。

## 结论

第 17 轮仍有问题，不能结束循环。

`phases/00-setup-and-tooling/02-git-and-collaboration` 没有代码目录，因此本轮不涉及 code/tests。主要问题是 quiz 仍是旧分布：5 题、`pre=2/post=3`、没有 `check` 阶段。中文文档整体可读，但 Mermaid 图示和术语表第一列保留普通英文标签，作为第一阶段第二课的核心概念页，读起来不够自然。

## Findings

### P1 - Git 与协作 quiz 是旧分布

文件：
- `phases/00-setup-and-tooling/02-git-and-collaboration/quiz.json`
- `phases/00-setup-and-tooling/02-git-and-collaboration/quiz.zh-CN.json`

证据：
- 中英 quiz 都只有 5 道题。
- 阶段分布为 `pre=2/post=3`，没有 `check`。
- 课程合同要求 6 道题：1 pre、3 check、2 post。
- quiz 文件也缺少 `lesson` 和 `title` 顶层字段。

影响：
- 站点可以渲染，但课程节奏不符合当前契约，且缺少课中检查。

### P2 - 中文图示和术语表保留普通英文标签

文件：
- `phases/00-setup-and-tooling/02-git-and-collaboration/docs/zh-CN.md`

证据：
- Mermaid 图示的参与者仍为 `Working Directory`、`Staging Area`、`Local Repo`、`Remote (GitHub)`。
- 关键术语表第一列仍为 `Commit`、`Branch`、`Merge`、`Remote`。

影响：
- 这些是核心概念标签，不是必须保留的命令或代码字段；中文页面应中文优先，并在必要时附英文术语。

## 第 17 轮建议修复范围

1. 将 Mermaid 可见标签本地化为工作目录、暂存区、本地仓库、远端。
2. 将术语表改为中文优先的双语术语，如 `提交（commit）`。
3. 将中英 quiz 改为 6 题，阶段分布为 `pre=1/check=3/post=2`，并补齐顶层 `lesson` 和 `title`。
4. 运行单课 i18n、全量校验和本地网页复验；本课修复作为单独 lesson commit。
